# OpenHands/OpenHands — 2026-03-25

> Autonomous software engineering agent; bellwether for agentic dev tooling.

**Window:** 2026-03-24T23:59:59.000Z → 2026-03-25T23:59:59.000Z  
**Default branch:** `main`  
**Source:** [github.com/OpenHands/OpenHands](https://github.com/OpenHands/OpenHands)

## Daily summary

> _LLM-generated. May contain errors — click through before acting._

Quiet day in OpenHands/OpenHands: no releases, no merged or newly opened PRs surfaced, and no notable issues — just four bug-fix commits landing on `main`, split evenly between frontend and backend/auth.

The most consequential change is on the auth/multi-tenancy side: `889754a` fixes conversation creation under API key authentication so that the conversation is now associated with the API key's `org_id` rather than (presumably) the caller's default org. Anyone running OpenHands in a multi-org deployment or building integrations against the API should read this one to understand the behavior change and check whether existing automations relied on the old org-resolution path (#13568).

Closely related, `0b93566` hardens user provisioning by cleaning up orphaned Keycloak users when signup is rejected due to a duplicate email (#13495). Previously a half-created Keycloak record could be left behind, which would block retries and gradually pollute the identity store; operators who have seen "user exists in Keycloak but not in app DB" support tickets will want to confirm this resolves them.

On the frontend, `3cd85a0` (APP-1093) makes the conversation UI correctly show a "Starting" status when the server reports `STARTING` on resume, instead of falling through to a misleading state (#13580). And `f7b5328` is a small defensive fix in the hooks modal to guard against `matcher.hooks` being undefined, which was likely throwing in the UI for some users (#13589).

No API surface changes, no schema migrations, and no dependency bumps are visible in this window. Reviewers triaging the day can safely skim the two frontend commits and focus their attention on #13568 and #13495, both of which touch identity/tenancy boundaries where regressions tend to be expensive. There is nothing here that should block a deploy, but the org_id fix is worth calling out in release notes for API consumers.

## Releases

_None in window._


## Merged PRs

_None in window._


## Open PRs (new or updated)

_None in window._


## Notable Issues

_None in window._


## Commits on `main`

- [`f7b5328`](https://github.com/OpenHands/OpenHands/commit/f7b53283b5692e1ba1755bfba8a51f09c76f5ec7) fix(frontend): guard against undefined matcher.hooks in hooks modal (#13589) — _@xingyaoww_
- [`3cd85a0`](https://github.com/OpenHands/OpenHands/commit/3cd85a07b78a2d9d2e28b2904e1b42096d47a802) APP-1093 fix(frontend): display 'Starting' status when server reports STARTING on conversation resume (#13580) — _@tofarr_
- [`0b93566`](https://github.com/OpenHands/OpenHands/commit/0b935669f35dff09303f42daf3e39c4cbb2678ed) fix(backend): clean up orphaned Keycloak users on duplicate email rejection (#13495) — _@hieptl_
- [`889754a`](https://github.com/OpenHands/OpenHands/commit/889754abfd214fa74138db496e06955edb323022) fix: use API key's org_id when creating conversations via API key auth (#13568) — _@hieptl_


---
_Generated by [oss-digest](https://github.com/Bojun-Vvibe/oss-digest) · v0.2 (LLM summary + deterministic detail)._
