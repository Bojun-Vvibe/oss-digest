# modelcontextprotocol/servers — 2026-02-28

> Reference MCP server implementations; canonical examples for the protocol.

**Window:** 2026-02-27T23:59:59.000Z → 2026-02-28T23:59:59.000Z  
**Default branch:** `main`  
**Source:** [github.com/modelcontextprotocol/servers](https://github.com/modelcontextprotocol/servers)

## Daily summary

> _LLM-generated. May contain errors — click through before acting._

A quiet day in `modelcontextprotocol/servers`: no releases, no new issues, and no newly opened or merged PRs surfaced in the window. The only activity of note is a single commit landing on `main`.

That commit, [`72efc42`](https://github.com/modelcontextprotocol/servers/commit/72efc429765fdde0c0c5fa561599ace789bcfbe4), merges PR #3230 ("fix(filesystem): mark move_file as destructive operation") from contributor `nulone`, with `olaservo` handling the merge. The change targets the reference **filesystem** server and corrects the tool annotation on `move_file` so that it is properly flagged as destructive. This matters for MCP clients that rely on tool hints (`destructiveHint`, `readOnlyHint`, etc.) to decide whether to auto-approve a call or to require explicit user confirmation — previously, a `move_file` invocation could slip through approval flows that gate destructive actions, even though moving (and potentially overwriting) a file clearly mutates the filesystem in a non-recoverable way for the original path.

For readers triaging what to look at: if you maintain a downstream client or a fork of the filesystem server, this is worth a quick glance to confirm your approval/guardrail logic keys off the corrected hint. There is no API or schema break — only the metadata advertised for the tool changes — so the upgrade should be drop-in. No other servers (git, github, memory, fetch, time, sqlite, etc.) saw activity in this window, and there is no movement on the broader issue backlog or any pending release cut to track.

Overall, today is essentially a single-line correctness fix to tool metadata; nothing else in the repo demands attention from the digest's audience.

## Releases

_None in window._


## Merged PRs

_None in window._


## Open PRs (new or updated)

_None in window._


## Notable Issues

_None in window._


## Commits on `main`

- [`72efc42`](https://github.com/modelcontextprotocol/servers/commit/72efc429765fdde0c0c5fa561599ace789bcfbe4) Merge pull request #3230 from nulone/fix/filesystem-move-file-destructive-hint — _@olaservo_


---
_Generated by [oss-digest](https://github.com/Bojun-Vvibe/oss-digest) · v0.2 (LLM summary + deterministic detail)._
