# cline/cline — 2026-04-18

> VS Code-native autonomous coding agent; another adjacent ecosystem signal.

**Window:** 2026-04-17T23:59:59.000Z → 2026-04-18T23:59:59.000Z  
**Default branch:** `main`  
**Source:** [github.com/cline/cline](https://github.com/cline/cline)

## Daily summary

> _LLM-generated. May contain errors — click through before acting._

A quiet day in `cline/cline` — no releases, no new issues, no direct commits to `main`, and only a handful of PR activity. Nothing landed; the noteworthy items are all still open or were closed without merging.

The most consequential item is the Aikido autofix bot's dependency-upgrade PR [#10311](https://github.com/cline/cline/pull/10311), which proposed bumping `protobufjs`, `node-forge`, `basic-ftp` and six other packages to remediate 21 critical vulnerabilities — including a code-injection RCE in protobufjs, a certificate-chain validation bypass in node-forge, and signature-forgery/DoS issues. The bot's own breaking-change analysis was incomplete (6 of 9 packages analyzed; `follow-redirects`, `@hono/node-server`, and `brace-expansion` were not assessed), and the PR was **closed without merging** the same day. If you care about Cline's supply-chain posture, this is worth a look to understand why it was rejected and whether a follow-up is planned.

A small but user-visible bug fix arrived in [#10315](https://github.com/cline/cline/pull/10315) (still open): Cline's `api.ts` has incorrect pricing metadata for `gemini-3-flash-preview` — `cacheWritesPrice` is set to 0.05 and `cacheReadsPrice` is missing entirely, which the code interprets as free cache reads and silently breaks cost accounting for that model. Tracked as issue #10314. Low risk, high value if you rely on Gemini cost reporting.

Finally, [#10241](https://github.com/cline/cline/pull/10241) saw further iteration: a fix for Mermaid diagram text labels being clipped when zooming or exporting to PNG (#7398). The earlier CSS-only fix in #8050 only addressed the inline view; this PR uses `getBBox()` to measure actual rendered bounds and expands the SVG `viewBox` so that overflowing labels survive the canvas-based PNG export path in `svgToPng()`.

Net: skim #10311 for the security/dependency story, glance at #10315 if you use Gemini, otherwise this day is safe to skip.

## Releases

_None in window._


## Merged PRs

_None in window._


## Open PRs (new or updated)

- [#10315](https://github.com/cline/cline/pull/10315) **Fix gemini flash 3 preview price** — _by @dirac-run, updated 2026-04-18T16:11:21Z_
- [#10241](https://github.com/cline/cline/pull/10241) **fix: Mermaid node text labels clipped when zooming/exporting to PNG** — _by @bob10042, updated 2026-04-18T13:26:22Z_


## Closed PRs (not merged)

- [#10311](https://github.com/cline/cline/pull/10311) **[Aikido] Fix 21 critical issues in protobufjs, node-forge, basic-ftp and 6 more** — _by @aikido-autofix[bot], closed 2026-04-18T22:47:35Z_


## Notable Issues

_None in window._


## Commits on `main`

_None in window._


---
_Generated by [oss-digest](https://github.com/Bojun-Vvibe/oss-digest) · v0.2 (LLM summary + deterministic detail)._
