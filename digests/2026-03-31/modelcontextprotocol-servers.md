# modelcontextprotocol/servers — 2026-03-31

> Reference MCP server implementations; canonical examples for the protocol.

**Window:** 2026-03-30T23:59:59.000Z → 2026-03-31T23:59:59.000Z  
**Default branch:** `main`  
**Source:** [github.com/modelcontextprotocol/servers](https://github.com/modelcontextprotocol/servers)

## Daily summary

> _LLM-generated. May contain errors — click through before acting._

A quiet day in `modelcontextprotocol/servers`: no releases, no new issues, no commits to `main`, and only one PR event of note — the closure (without merge) of PR [#3663](https://github.com/modelcontextprotocol/servers/pull/3663).

## SQLite injection fix closed unmerged

The single notable activity is the closure of [#3663](https://github.com/modelcontextprotocol/servers/pull/3663) ("fix(sqlite): prevent SQL injection in describe_table and query tools") on 2026-03-31, ten days after it was opened. The PR, submitted by `xr843`, targeted what its author described as a critical SQL injection vulnerability (OWASP A03:2021) in the SQLite reference server's `describe_table` tool, where the `table_name` parameter was interpolated directly into a PRAGMA query via an f-string with no sanitization. The proposed fix added defense-in-depth: a regex identifier check, a whitelist validation against `sqlite_master`, and bracket quoting. It claimed to close issue #3314.

The PR was closed without being merged and the available payload includes no maintainer comment explaining why. Readers tracking SQLite-server security should check the PR thread directly to see whether the issue is considered out of scope (the SQLite server now lives in the archived reference set), already addressed elsewhere, or rejected on technical grounds. If you maintain a fork or downstream deployment of the SQLite MCP server, the vulnerability description in the PR body is worth reading on its own merits regardless of merge status.

## Everything else

No releases were cut, no commits landed on `main`, no new PRs were opened, and no issues were filed or flagged in the window. Activity in this repo continues to taper as the official servers reorganization pushes contributors toward per-server repos and third-party registries — a trend worth keeping an eye on if you depend on any of the in-tree reference implementations for production use.

Nothing here is urgent to read in full unless you specifically care about the SQLite server's security posture, in which case [#3663](https://github.com/modelcontextprotocol/servers/pull/3663) and referenced issue #3314 are the two threads to open.

## Releases

_None in window._


## Merged PRs

_None in window._


## Open PRs (new or updated)

_None in window._


## Closed PRs (not merged)

- [#3663](https://github.com/modelcontextprotocol/servers/pull/3663) **fix(sqlite): prevent SQL injection in describe_table and query tools** — _by @xr843, closed 2026-03-31T11:33:55Z_


## Notable Issues

_None in window._


## Commits on `main`

_None in window._


---
_Generated by [oss-digest](https://github.com/Bojun-Vvibe/oss-digest) · v0.2 (LLM summary + deterministic detail)._
