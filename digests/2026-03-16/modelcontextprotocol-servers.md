# modelcontextprotocol/servers — 2026-03-16

> Reference MCP server implementations; canonical examples for the protocol.

**Window:** 2026-03-15T23:59:59.000Z → 2026-03-16T23:59:59.000Z  
**Default branch:** `main`  
**Source:** [github.com/modelcontextprotocol/servers](https://github.com/modelcontextprotocol/servers)

## Daily summary

> _LLM-generated. May contain errors — click through before acting._

The day's notable change is a Windows path-handling fix in the filesystem server: [#3434](https://github.com/modelcontextprotocol/servers/pull/3434) by Chelebii was merged (commit [b60eca1](https://github.com/modelcontextprotocol/servers/commit/b60eca1b3bfcd12e2afe7000882db363a1b89f6e)), ensuring bare Windows drive letters like `C:` normalize to the drive root (`C:\`) rather than `C:.`. The root cause was Node's `path.normalize()` treating a bare drive letter as the current directory on that drive, which broke downstream path validation in the filesystem MCP server. The fix appends `path.sep` to bare drive letters before normalization. If you maintain Windows-targeted MCP integrations or downstream tools that pass user-supplied roots into the filesystem server, this is worth a quick review to confirm no callers depended on the prior `C:.` output.

The other activity of note is a closed-but-not-merged PR against the gitlab server: [#3600](https://github.com/modelcontextprotocol/servers/pull/3600) from nielskaspers, targeting issue #3454. It addresses a crash in `search_repositories` (`McpError: Cannot read properties of undefined (reading 'map')`) seen on self-hosted GitLab Enterprise v18.4, where the `/projects` endpoint can return a non-array body that then trips Zod's `z.array()` validator. The proposed fix is a nullish-coalescing guard before validation. The PR was closed within a minute of opening with no merge — likely a duplicate, mis-targeted branch, or superseded submission rather than a rejection on technical grounds; maintainers of GitLab Enterprise deployments hitting #3454 may want to track whether a follow-up PR appears, since the underlying bug remains relevant.

No releases shipped, no new issues were filed, and no other commits landed in the window. Overall a quiet day dominated by a single small but real correctness fix on Windows; readers short on time can stop at #3434.

## Releases

_None in window._


## Merged PRs

- [#3434](https://github.com/modelcontextprotocol/servers/pull/3434) **fix(filesystem): ensure bare Windows drive letters normalize to root** — _by @Chelebii, merged 2026-03-16T19:12:01Z_


## Open PRs (new or updated)

_None in window._


## Closed PRs (not merged)

- [#3600](https://github.com/modelcontextprotocol/servers/pull/3600) **fix(gitlab): handle undefined response in search_repositories** — _by @nielskaspers, closed 2026-03-16T07:10:28Z_


## Notable Issues

_None in window._


## Commits on `main`

- [`b60eca1`](https://github.com/modelcontextprotocol/servers/commit/b60eca1b3bfcd12e2afe7000882db363a1b89f6e) fix(filesystem): ensure bare Windows drive letters normalize to root (#3434) — _@Chelebii_


---
_Generated by [oss-digest](https://github.com/Bojun-Vvibe/oss-digest) · v0.2 (LLM summary + deterministic detail)._
