# modelcontextprotocol/servers — 2026-04-13

> Reference MCP server implementations; canonical examples for the protocol.

**Window:** 2026-04-12T23:59:59.000Z → 2026-04-13T23:59:59.000Z  
**Default branch:** `main`  
**Source:** [github.com/modelcontextprotocol/servers](https://github.com/modelcontextprotocol/servers)

## Daily summary

> _LLM-generated. May contain errors — click through before acting._

A quiet 24 hours in modelcontextprotocol/servers: no releases, no commits to `main`, and no new notable issues. All activity was triage on existing PRs, none of which merged in-window.

The most consequential item is the closure (without merge) of **#3611**, a fix for a `search_repositories` crash in the GitLab server against self-hosted/Enterprise GitLab (e.g. v18.4). The bug is real: GitLab's `GET /projects?search=` returns a bare array on gitlab.com but an object wrapper on some self-hosted instances, causing Zod validation to call `.map()` on `undefined`. The PR added a null-safety normalisation guard. Worth a look if you run the GitLab server against Enterprise — the underlying defect remains unaddressed in `main`, so users on self-hosted GitLab should expect the crash until a maintainer-accepted fix lands.

Also closed without merge: **#3911**, a proposal to list "Cortex," a third-party persistent ontology-driven knowledge system (OWL-RL reasoning, SPARQL + SQLite, 22 MCP tools, local-first, 1,010 tests). Closure here is consistent with the repo's general posture on third-party server submissions; relevant only if you're tracking the broader MCP server ecosystem rather than this repo itself.

Finally, **#3229** (filesystem: gracefully handle unavailable directories, fixes #2815) saw activity but was already merged back on 2026-02-06 — today's update is just follow-up discussion. The behaviour change it shipped (filter inaccessible dirs with a warning, only fail if none are accessible) is worth remembering if you're debugging filesystem-server startup on machines with unmounted external drives.

Net takeaway: nothing shipped, no new surface area to learn. The only item warranting a click-through is **#3611** if you operate against self-hosted GitLab — both to understand the failure mode and because a maintainer-accepted patch will likely follow.

## Releases

_None in window._


## Merged PRs

- [#3229](https://github.com/modelcontextprotocol/servers/pull/3229) **fix(filesystem): gracefully handle unavailable directories** — _by @nulone, merged 2026-02-06T13:38:31Z_


## Open PRs (new or updated)

_None in window._


## Closed PRs (not merged)

- [#3611](https://github.com/modelcontextprotocol/servers/pull/3611) **fix(gitlab): guard against undefined response in search_repositories** — _by @joaquinhuigomez, closed 2026-04-13T21:02:49Z_
- [#3911](https://github.com/modelcontextprotocol/servers/pull/3911) **Add Cortex — persistent, ontology driven knowledge system with OWL-RL reasoning** — _by @abbacusgroup, closed 2026-04-12T21:16:48Z_


## Notable Issues

_None in window._


## Commits on `main`

_None in window._


---
_Generated by [oss-digest](https://github.com/Bojun-Vvibe/oss-digest) · v0.2 (LLM summary + deterministic detail)._
