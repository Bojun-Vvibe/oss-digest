# modelcontextprotocol/servers — 2026-04-16

> Reference MCP server implementations; canonical examples for the protocol.

**Window:** 2026-04-15T23:59:59.000Z → 2026-04-16T23:59:59.000Z  
**Default branch:** `main`  
**Source:** [github.com/modelcontextprotocol/servers](https://github.com/modelcontextprotocol/servers)

## Daily summary

> _LLM-generated. May contain errors — click through before acting._

Quiet day on modelcontextprotocol/servers: no releases, no commits to `main`, no new issues, and every PR touched in the window was **closed without merge**. The clear signal is that maintainers are doing housekeeping rather than landing code, consistent with the project's standing notice that new server submissions belong in the [MCP Server Registry](https://github.com/modelcontextprotocol/registry) rather than this repo's README.

The most visible action is the rejection of yet another README-addition PR, "Add Maasy AI Marketing Copilot MCP server" (#3958), closed within hours of being opened with the standard registry-redirect message. Contributors looking to list servers should route there instead.

More interesting for users actually running the reference servers is a batch of bug-fix PRs that were closed unmerged the same day, presumably superseded or rolled into other work — worth checking whether the underlying bugs are still live before relying on these servers:

- **server-memory**: #2808 (jikanter) fixed a reversed `includes()` call that broke entity search on `main`; #3094 (jared03) stripped the stored `type` field from entities/relations in `loadGraph` to fix `read_graph`/`search_nodes`/`open_nodes` failing with `MCP error -32602` schema-mismatch errors; #3161 (majiayu000) proposed `.passthrough()` on `EntitySchema`/`RelationSchema` so entries with extra properties (e.g. `custom_id`, per issue #3144) wouldn't blow up `read_graph` validation.
- **server-git**: #3156 (schuyler) replaced GitPython's `index.add()` with the `git` CLI inside `git_add` to dodge a known GitPython index-corruption bug that staged the `.git` directory and prefixed paths with `./` when callers passed mixed inputs like `[".", "other.md"]`.

All four bug fixes describe concrete, reproducible failure modes in the memory and git reference servers. Since none landed, anyone hitting `MCP error -32602` from the memory server, broken entity search, or corrupted commits from `git_add` should track whether replacement PRs exist or apply these patches locally. No notable issues were filed in the window, so there's nothing else to triage today.

## Releases

_None in window._


## Merged PRs

_None in window._


## Open PRs (new or updated)

_None in window._


## Closed PRs (not merged)

- [#3958](https://github.com/modelcontextprotocol/servers/pull/3958) **Add Maasy AI Marketing Copilot MCP server** — _by @Jbelieve, closed 2026-04-16T05:04:36Z_
- [#2808](https://github.com/modelcontextprotocol/servers/pull/2808) **Fix the entity search by reversing includes call** — _by @jikanter, closed 2026-04-16T01:39:35Z_
- [#3094](https://github.com/modelcontextprotocol/servers/pull/3094) **fix(memory): strip type field from entities/relations in loadGraph** — _by @jared03, closed 2026-04-16T01:39:19Z_
- [#3161](https://github.com/modelcontextprotocol/servers/pull/3161) **fix(memory): allow additional properties in entity/relation schemas** — _by @majiayu000, closed 2026-04-16T01:19:04Z_
- [#3156](https://github.com/modelcontextprotocol/servers/pull/3156) **fix(git): use git CLI in git_add to avoid GitPython index corruption bug** — _by @schuyler, closed 2026-04-16T01:18:52Z_


## Notable Issues

_None in window._


## Commits on `main`

_None in window._


---
_Generated by [oss-digest](https://github.com/Bojun-Vvibe/oss-digest) · v0.2 (LLM summary + deterministic detail)._
