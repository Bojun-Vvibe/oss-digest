# modelcontextprotocol/servers — 2026-03-26

> Reference MCP server implementations; canonical examples for the protocol.

**Window:** 2026-03-25T23:59:59.000Z → 2026-03-26T23:59:59.000Z  
**Default branch:** `main`  
**Source:** [github.com/modelcontextprotocol/servers](https://github.com/modelcontextprotocol/servers)

## Daily summary

> _LLM-generated. May contain errors — click through before acting._

Quiet day on modelcontextprotocol/servers: no releases, no new issues, no commits to `main`, and every PR touched in the window was **closed without merge**. The activity is dominated by a batch of "ghost"-authored fixes against `memory`, `filesystem`, and `git` servers that were all closed on 2026-03-26 — worth a look if you depend on any of those servers, since the underlying bugs likely remain open.

The most consequential item is the security PR [#3724](https://github.com/modelcontextprotocol/servers/pull/3724) from `dagecko` (Vigilant / Runner Guard), proposing to pin 8 unpinned GitHub Actions to mitigate the same supply-chain vulnerability class exploited in the recent tj-actions / Trivy / LiteLLM attacks. It was closed the same day without merge — maintainers may want to revisit, as the hardening is cheap and the threat model is current.

The cluster of closed `ghost` PRs describes concrete, well-documented bugs that users will likely re-encounter:

- [#3634](https://github.com/modelcontextprotocol/servers/pull/3634) memory: `read_graph`/`search_nodes`/`open_nodes` throw `MCP error -32602` when `memory.jsonl` contains entities with extra properties not in `EntitySchema` (e.g. legacy `custom_id`).
- [#3627](https://github.com/modelcontextprotocol/servers/pull/3627) memory: `outputSchema` is declared as a plain object containing Zod values instead of a `z.object()`, breaking SDK serialization for all 9 tools.
- [#3633](https://github.com/modelcontextprotocol/servers/pull/3633) filesystem: UNC paths like `\\192.168.4.96\Mega` work at root but subdirectory access is rejected because `isPathWithinAllowedDirectories` uses `path.resolve` instead of the codebase's UNC-preserving `normalizePath()`.
- [#3632](https://github.com/modelcontextprotocol/servers/pull/3632) filesystem: Windows `EPERM` on `fs.rename` when the target file is locked (e.g. open in VS Code) breaks `write_file`/`edit_file`; proposes an `atomicReplace()` fallback.
- [#3631](https://github.com/modelcontextprotocol/servers/pull/3631) git: `repo_path` is only validated against the CLI `--repository` flag, so when unset, all paths are allowed even after a client removes a root at runtime — a quiet authorization gap.

Also closed without merge: [#3506](https://github.com/modelcontextprotocol/servers/pull/3506), a community-server listing for `BoJ Server`. No maintainer rationale is visible in the data for any of the closures.

## Releases

_None in window._


## Merged PRs

_None in window._


## Open PRs (new or updated)

_None in window._


## Closed PRs (not merged)

- [#3724](https://github.com/modelcontextprotocol/servers/pull/3724) **fix: pin 8 unpinned action(s)** — _by @dagecko, closed 2026-03-26T21:22:45Z_
- [#3506](https://github.com/modelcontextprotocol/servers/pull/3506) **Add BoJ Server to Community Servers** — _by @hyperpolymath, closed 2026-03-20T18:27:29Z_
- [#3634](https://github.com/modelcontextprotocol/servers/pull/3634) **fix(memory): strip extra properties from entities/relations to fix schema validation** — _by @ghost, closed 2026-03-26T14:12:37Z_
- [#3633](https://github.com/modelcontextprotocol/servers/pull/3633) **fix(filesystem): fix UNC path subdirectory access denied** — _by @ghost, closed 2026-03-26T14:12:37Z_
- [#3632](https://github.com/modelcontextprotocol/servers/pull/3632) **fix(filesystem): handle Windows EPERM on file rename when target is locked** — _by @ghost, closed 2026-03-26T14:12:36Z_
- [#3631](https://github.com/modelcontextprotocol/servers/pull/3631) **fix(git): validate repo_path against current roots on every tool call** — _by @ghost, closed 2026-03-26T14:12:36Z_
- [#3627](https://github.com/modelcontextprotocol/servers/pull/3627) **fix(memory): use plain JSON Schema for outputSchema instead of Zod objects** — _by @ghost, closed 2026-03-26T14:12:36Z_


## Notable Issues

_None in window._


## Commits on `main`

_None in window._


---
_Generated by [oss-digest](https://github.com/Bojun-Vvibe/oss-digest) · v0.2 (LLM summary + deterministic detail)._
