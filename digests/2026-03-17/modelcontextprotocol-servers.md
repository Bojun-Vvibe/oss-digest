# modelcontextprotocol/servers — 2026-03-17

> Reference MCP server implementations; canonical examples for the protocol.

**Window:** 2026-03-16T23:59:59.000Z → 2026-03-17T23:59:59.000Z  
**Default branch:** `main`  
**Source:** [github.com/modelcontextprotocol/servers](https://github.com/modelcontextprotocol/servers)

## Daily summary

> _LLM-generated. May contain errors — click through before acting._

A quiet day in `modelcontextprotocol/servers`: no releases, no new issues, and only a single merged PR touching the Python `fetch` package's lockfile.

The sole change of note is [#3598](https://github.com/modelcontextprotocol/servers/pull/3598) by Chelebii, merged at 14:21 UTC as commit [`f424458`](https://github.com/modelcontextprotocol/servers/commit/f4244583a6af9425633e433a3eec000d23f4e011). It refreshes `src/fetch/uv.lock` so that `uv sync --locked --all-extras --dev` once again matches the project metadata, unblocking CI for the fetch server. The author kept the scope deliberately narrow — only the lockfile in `src/fetch` — and verified locally that `uv sync --locked --all-extras --dev` succeeds in `src/fetch`, `src/git`, and `src/time`. This work was split out from the broader filesystem fix in #3434 specifically to keep the lockfile bump easy to review and land independently.

There is nothing else to triage today: no new open PRs were filed in the window, no issues were opened or flagged as notable, and no releases were cut. Maintainers and downstream packagers can safely skip a deep read unless they consume the `fetch` package directly from source — in which case pulling `main` will resolve any `uv sync --locked` failures they were seeing. Contributors watching #3434 should note that the lockfile prerequisite is now in, so that PR can be rebased and refocused on the underlying filesystem fix.

Recommended reading: only [#3598](https://github.com/modelcontextprotocol/servers/pull/3598), and only if you maintain or build against `src/fetch`.

## Releases

_None in window._


## Merged PRs

- [#3598](https://github.com/modelcontextprotocol/servers/pull/3598) **fix(fetch): refresh uv lockfile for CI** — _by @Chelebii, merged 2026-03-17T14:21:11Z_


## Open PRs (new or updated)

_None in window._


## Notable Issues

_None in window._


## Commits on `main`

- [`f424458`](https://github.com/modelcontextprotocol/servers/commit/f4244583a6af9425633e433a3eec000d23f4e011) fix(fetch): refresh uv lockfile (#3598) — _@Chelebii_


---
_Generated by [oss-digest](https://github.com/Bojun-Vvibe/oss-digest) · v0.2 (LLM summary + deterministic detail)._
