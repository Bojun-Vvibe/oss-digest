# W17 Synthesis #312 — Opencode n=4 drop-out streak breaks the prior W17 n=3 ceiling and joins goose (n=7) in a newly-formed deep-dormancy class, establishing a bipolar repo-state distribution where 2-of-6 repos are deep-dormant concurrently while codex+litellm carry the entire active-repo activity load via the synth #311 backbone-restoration rebound

**Thesis.** The Add.119-140 band has tracked per-repo dormancy via a single ceiling: prior W17 record was qwen-code at n=3 consecutive zero-merge ticks (Add.122-124), matched by qwen-code again at Add.136-138 and by opencode joining at Add.137-139 per ADDENDUM-139 ("opencode now matches the qwen-code prior W17 record at n=3 drop-out streak"). Goose has been an outlier extending the record monotonically from n=3 (Add.135) → n=4 (Add.136) → n=5 (Add.137) → n=6 (Add.138) → n=7 (Add.139) with no break, and now n=7 sustained at Add.140 for the **6th consecutive record-extension tick**. Until Add.140, goose was the **lone repo above the n=3 ceiling** — the deep-dormancy class had cardinality 1, and could be read as a goose-specific anomaly. ADDENDUM-140 changes the cardinality: opencode emits **0 in-window merges** (window 02:25:14Z → 03:20:36Z, depth post-Hona #24864 now 3h42m+), advancing its drop-out streak to **n=4** and **breaking the prior W17 n=3 ceiling for the first repo other than goose**. The deep-dormancy class (n≥4 consecutive zero-merge ticks) now has cardinality **2** (goose at n=7, opencode at n=4), and the implication is structural: the n=3 ceiling is **not a corpus-wide invariant** but a **per-repo soft ceiling that breaks under sustained back-pressure**. Concurrent with this deep-dormancy expansion, codex and litellm execute the synth #311 backbone-restoration rebound (codex 3 merges, litellm 4 merges, joint 7-of-8 = 87.5% of cross-repo activity), and qwen-code emits its lone-merger at n=1 (pomelo-nwu #3577 `3ea81a1a` `feat(skills): add tmux-real-user-testing skill for readable TUI test logs`) with author-rotation from the Add.139 wenshao anchor. The resulting **bipolar repo-state distribution at Add.140** is: 2 repos active-high-rate (codex 3, litellm 4), 1 repo active-low-rate (qwen-code 1), 1 repo shallow-dormant (gemini-cli ~2h07m+ at lag-3 singleton-then-redormant), and **2 repos deep-dormant** (opencode n=4, goose n=7). This is the first observed Add.119-140 tick with **2 repos in the deep-dormancy class concurrently**, and the first with **n≥4 deep-dormancy on a non-goose repo**.

**Refinements over prior synths.**
- **Refines ADDENDUM-139's framing** of opencode at n=3 as "matches prior W17 qwen-code record" → "**n=3 was a soft ceiling, broken at lag-1** by opencode itself extending to n=4; goose was not the only repo capable of breaking it".
- **Bounds the qwen-code n=3 ceiling reading** of ADDENDUM-139 (qwen-code n=3 streak terminated at exactly the prior record without extension) to a **per-repo property of qwen-code at that specific occasion**, not a corpus-wide invariant. Opencode now establishes that other repos can extend past n=3 without terminating, refining the n=3 reading from "ceiling" to "soft threshold".
- **Cross-references synth #167** (single-window cross-repo cardinality halving via sst/opencode silent-window debut and dual-track cadence coupling reveal): synth #167 noted opencode's silent-window-debut as a discriminating event; synth #312 extends this from a single-tick silent-window debut to a **multi-tick deep-dormancy class membership** at n=4.
- **Cross-references synth #216** (cross-repo silence duration ratio establishing 4-tier per-repo dormancy regime crossing 48h boundary): synth #216 captured per-repo dormancy as durations (gemini-cli 59h, litellm 30h, codex 2.9h, opencode 0.1h at single capture); synth #312 reframes per-repo dormancy as **consecutive-zero-merge-tick counts**, which is a discrete-tick-count metric distinct from synth #216's wall-clock duration metric. The two metrics co-vary (longer durations → more consecutive zero-merge ticks) but are not identical because tick widths vary (Add.139 was 21m29s, Add.140 is 55m22s — a 2.58× span ratio), so a single zero-merge tick at a wide window represents more wall-clock dormancy than a zero-merge tick at a narrow window.
- **Cross-references synth #246-256-258** (cross-repo synchronous merge pause regime, stratified resumption, deep cohort cardinality conservation): synth #246-256-258 framed cross-repo dormancy at the corpus level via "pause-phase" and "deep cohort" metrics. Synth #312 sharpens the deep-cohort metric at Add.140: cohort cardinality is **2** (goose + opencode), distinct from the synth #258 deep-cohort cardinality conservation at 3 (codex + qwen-code + goose at Add.110-band) — the cardinality has **decreased to 2** but the **identity has shifted** (codex left via Add.140 silence-break-rebound, qwen-code left via Add.139-140 lone-merger, opencode joined via Add.137-140 streak extension). The deep-cohort identity is therefore **non-stationary across the W17 band**, not a fixed roster.
- **Establishes new meta-rule M-312.M**: **W17 deep-dormancy class membership (n≥4 consecutive zero-merge ticks) is a non-stationary set of cardinality ≥1 that admits per-repo entries via streak-extension past the n=3 soft threshold**. Two instances confirm: goose (entered via Add.136 at n=4 from Add.135 n=3 baseline, sustained through Add.140 n=7); opencode (entered via Add.140 at n=4 from Add.137-139 n=3 streak). A third instance (e.g., qwen-code re-entering deep-dormancy via Add.141+ extension, or gemini-cli entering deep-dormancy if its current ~2h07m+ singleton-then-redormant cadence runs n=4 ticks) would elevate this to a stable W17 invariant.

**Cited PRs / commits.**
- Opencode last merge (n=4 drop-out anchor): Hona **#24864** at Add.135 (depth post-Hona #24864 now **3h42m+** at Add.140 capture).
- Goose last merge (n=7 drop-out anchor): morgmart **#8881** (depth post-morgmart #8881 now **5h47m+** at Add.140 capture).
- Synth #312 contrastive citations (active-repo-load): codex bolinfest **#20106** `e6db1a94` 02:43:44Z + xl-openai **#20112** `6f328d5e` 02:56:26Z + starr-openai **#20058** `e1ec9e63` 03:05:18Z; litellm yuneng-berri **#26734** `804e7c0c` 02:44:58Z + ishaan-berri **#26705** `4ae2996f` 03:10:42Z + xinrui-z **#24294** `44ab0167` 03:18:31Z + Sameerlite **#26737** `b5161200` 03:20:13Z; qwen-code pomelo-nwu **#3577** `3ea81a1a` 03:19:00Z. The 7 active-repo merges in codex+litellm represent **87.5% of cross-repo Add.140 activity** while 2 repos sit in the deep-dormancy class — the bipolar distribution is **maximally polarized at Add.140**.
- Gemini-cli last merge (lag-3 singleton-then-redormant, NOT yet deep-dormant): #26150 (depth ~2h07m+, n=3 zero-merge tick streak — at the n=3 soft threshold, candidate for deep-dormancy entry at Add.141).

**Predictions / falsifiers.**
- P-312.A: opencode remains at zero merges for ≥1 more tick (Add.141), advancing the drop-out streak to **n=5** and consolidating its deep-dormancy class membership. Falsifier: any opencode merge in Add.141.
- P-312.B: goose remains at zero merges for ≥1 more tick (Add.141), advancing the drop-out streak to **n=8** and extending the W17 record for the 7th consecutive tick. Falsifier: any goose merge in Add.141.
- P-312.C (cardinality): the deep-dormancy class cardinality at Add.141 is **≥2** (goose + opencode both still in, OR opencode breaks but a different repo enters via streak-extension). Falsifier: cardinality drops to ≤1 at Add.141 (i.e., both opencode AND no other repo exceed n=3 at Add.141).
- P-312.D (gemini-cli candidate entry): gemini-cli's current n=3 zero-merge streak (post-#26150) extends to n=4 at Add.141, making gemini-cli the **third repo** to enter the deep-dormancy class within the Add.119-141 band. Falsifier: gemini-cli emits ≥1 merge at Add.141.
- P-312.E (M-312.M test): if a third repo enters deep-dormancy via streak-extension past n=3 within W17, M-312.M elevates from a 2-instance pattern to a stable W17 invariant. Falsifier: no third repo enters deep-dormancy through end-of-W17.

**Bipolar distribution shape note.** The Add.140 repo-state distribution (2 active-high / 1 active-low / 1 shallow-dormant / 2 deep-dormant) is the **most polarized** state observed in the Add.119-140 band: the active-high pair (codex 3 + litellm 4 = 7 merges) and the deep-dormant pair (opencode n=4 + goose n=7) are at opposite extremes simultaneously, with qwen-code (1 merge) and gemini-cli (n=3 at threshold) bridging the middle. Earlier band ticks (Add.124, Add.139) had **single-active-repo** trough states but only **one** repo in deep-dormancy (or zero, when goose was below n=4). Add.140 is the first tick to combine **a non-trough active state (3 active repos)** with **2 concurrent deep-dormancy entries**. This combination falsifies any reading of deep-dormancy as a corpus-level back-pressure phenomenon (which would predict deep-dormancy correlates with low active-repo-count), and instead supports a reading of deep-dormancy as a **per-repo back-pressure phenomenon decoupled from corpus-level activity**. The codex+litellm rebound and the opencode/goose deep-dormancy are **structurally independent processes** at Add.140 — synth #167's "dual-track cadence coupling reveal" generalizes here from 2 tracks to **k tracks where k = number of repos**, with each repo's dormancy/activity state evolving on its own track and the cross-repo distribution being the joint state across all k tracks rather than a corpus-wide single-state.
