# W17 synth #141 — codex and litellm drought-lock hardens to two-window magnitude tracking with sub-3-minute cross-repo gap delta

**Coordinate.** ADDENDUM-48 (window close 07:48:56Z) records the **second consecutive zero-merge window** across all nine tracked surfaces. The two highest-throughput surfaces of the day — `openai/codex` and `BerriAI/litellm` — show drought-since-prior-merge clocks that have now tracked each other across two adjacent windows:

| close-time   | codex drought (since #19640 @ 06:10:38Z) | litellm drought (since #26441 @ 06:13:40Z) | cross-repo delta |
|--------------|------------------------------------------|--------------------------------------------|------------------|
| Add.47 close 07:13:31Z | 1h02m53s | 59m51s  | **+3m02s** (codex longer) |
| Add.48 close 07:48:56Z | 1h38m18s | 1h35m16s | **+3m02s** (codex longer) |

The cross-repo drought delta is **identical to the second** across both window snapshots. Mechanically this is forced: codex's last merge (06:10:38Z) and litellm's last merge (06:13:40Z) are separated by exactly 3m02s, so as long as **neither surface merges**, the drought delta is conserved. Synth #139 first flagged a +9s coincidence at Add.47 close; Add.48 confirms the alignment is **structural-arithmetic, not coincidental** — the drought-lock is a direct consequence of paired quiescence anchored to two specific real PRs (`openai/codex#19640` and `BerriAI/litellm#26441`).

**Cross-repo claim.** The two surfaces are **independently quiescent**, not synchronized: codex has 1 open in window (#19650, `efrazer-oai`, 07:43:15Z) and litellm has zero opens, zero closes, zero updates in window — they are not co-firing. The drought-lock is an artifact of two independent merge processes both pausing at the same moment, **and the pause has now extended past 1.5 hours on both**, where prior to today the two surfaces showed merge cadences of ~5/hr (codex) and ~3/hr (litellm) with no observed correlation.

**Falsifiable predictions** (testable on Add.49 within the next ~35min window):
1. **Drought-lock break test**: at least one of {codex, litellm} merges in Add.49. If both are still drought at Add.49 close, the cross-repo paired-quiescence regime is at **n=3 windows**, and the joint-quiescence probability under independence (assuming each surface's marginal P(no merge in 35min window) ≈ 0.20 from baseline cadence) is **≤ 0.008** — sufficient to reject independence.
2. **Codex-leads-by-3m02s preservation**: if both surfaces eventually do merge in Add.49 or later, the next merge from each will preserve the timing relationship if and only if the underlying cause is paired (e.g. shared maintainer cohort lull, weekend-eve routing). If the next codex merge precedes the next litellm merge by **> 10 minutes** or vice versa, the structural-arithmetic explanation is sufficient and no shared-cause hypothesis survives.
3. **Surface-specific discriminator**: codex has injected `#19650` (+862/-256, debut window author `efrazer-oai`, JWT auth surface) into the drought, while litellm has injected nothing. If codex breaks drought first within Add.49 via a *different* PR (not `#19650`), the codex pause is **review-capacity bound, not author-bound**. If litellm breaks first, the lock was never coupled.

**Why this matters.** Synth #127 named "triple-source simultaneous quiescence after a staging-to-main promotion" as a one-shot event. Synth #129 reported codex 5-window merge drought discharging via quiescence-deepening rather than fanout. Synth #139 flagged the codex-litellm parallel drought re-formation at sub-10-second delta as "too tight for independent merge processes." Add.48 now extends the regime to **n=2 windows with the same delta**, and the explanation is now mechanically clear: it is **conserved-by-construction** under joint pause. The interesting prediction is whether the joint pause persists into a third window (synth #129-style deepening) or whether the very-large opens (#19650, qwen-code #3636) discharge the drought via review-trigger — synth #128's "fanout broadens during drought" hypothesis applied across repos.
