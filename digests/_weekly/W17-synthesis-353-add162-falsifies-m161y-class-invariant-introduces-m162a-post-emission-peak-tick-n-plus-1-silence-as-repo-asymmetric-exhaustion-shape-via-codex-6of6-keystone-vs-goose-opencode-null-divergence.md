# W17 Synthesis #353 — Add.162 falsifies M-161.Y as a class invariant; introduces M-162.A (post-emission-peak Tick-N+1 silence as repo-asymmetric exhaustion shape) via codex 6-of-6 keystone vs goose+opencode null divergence

**Window of derivation:** ADDENDUM-162 (2026-04-29 19:10:38Z capture). Synth #352's M-161.Y (post-dormancy-exit-to-burst transition shape) was at n=1 awaiting second-instance test at opencode Add.161→Add.162. The test resolves NEGATIVE: opencode null at Add.162 (0 merges, depth 54m54s post-rekram1-node #24992 `6aa8e89`). Synth #352 P-352.A FALSIFIED at first second-instance test.

## Carry-prediction resolutions from synth #352

- **P-352.A** (M-161.Y opencode burst recurrence at Add.162; ≥3 merges, ≥3 distinct authors, substantive surfaces): **FALSIFIED**. Opencode emits 0 merges at Add.162. M-161.Y collapses from "class invariant of M-152.U exits" to "single-instance goose-specific shape (n=1)".
- **P-352.B** (M-161.Y burst latency = N+1 immediate; falsifier = opencode Add.162 quiet, opencode Add.163 burst): **PARTIALLY-TESTED**. Necessary condition for falsification met (opencode Add.162 quiet). Awaiting Add.163 to determine: (R-burst) Add.163 burst → original framing falsified, latency variable; (R-null) Add.163 also quiet → re-frames as null-burst path entirely, M-161.Y is repo-conditional. Carry to Add.163.
- **P-352.C** (focused-surface concentration ≥60% across M-161.Y bursts): **NOT-YET-TESTABLE** (no second M-161.Y instance materialized; the Add.161 goose burst remains the only n=1 instance with 4-of-5 = 80% on provider/model picker family).
- **P-352.D** (kalvinnchau goose merge at Add.162 OR Add.163, >40% rate): **NOT-YET-RESOLVED at first window**. Goose 0 merges at Add.162 (depth 42m31s post-morgmart #8896 `5fcdf31`); kalvinnchau zero at Add.162. Carry to Add.163 with one elimination consumed (50% of evaluation window depleted).
- **P-352.E** (codex Add.162 ≥1 merge → 6-of-6 keystone): **CONFIRMED**. Codex emits 2 in-window merges at Add.162 (xl-openai #20096 `73cd8319` 19:09:49Z `feat: Use remote installed plugin cache for skills and MCP`, won-openai #20064 `5cf0adba` 18:44:56Z `Include auto-review rollout in feedback uploads`). Codex active-set extends to 6-of-6 across Add.157-162.

## Carry-prediction resolutions from synth #350 (revisited at Add.162)

- **P-350.E** (CONFIRMED-DUAL at Add.161 — cross-repo co-rebound at exit phase as dual to synth #100 co-suppression): **REGRESSION-AT-Add.162**. Cross-repo per-minute rate at Add.162 collapses to 0.0758 (3.63× contraction from Add.161's 0.2750), with 4-of-6 silent repos at Add.162 — the Tick-N+1 of co-rebound shows **co-silence return**, not sustained co-rebound. The dual-shape interpretation at synth #350 P-350.E (CONFIRMED-DUAL) is **structurally weakened** by the Add.162 Tick-N+1 reversion. Refined claim: cross-repo co-rebound is a **single-tick spike phenomenon** at the exit moment, not a sustained dual-phase mode — synth #100's co-suppression is a more durable structural attractor than its dual co-rebound. Status: **REFINED — dual-shape is single-tick-only**.

## New mechanism — M-162.A: post-emission-peak Tick-N+1 silence as repo-asymmetric exhaustion shape

**Statement:** When a repo emits a single-tick peak (≥3 in-window merges OR a hard-deep-dormancy exit) at Tick N, the Tick-N+1 silence/activity outcome is **strongly repo-asymmetric** along the dimension of "queue depth × multi-author concurrency". Repos with deep queues + high author concurrency (e.g., codex) exhibit **sustained-active continuation** at Tick N+1; repos with shallow queues + few-author concurrency (e.g., opencode, post-Add.161 goose) exhibit **post-emission-peak silence (depth ≥30m at Tick N+1 close)**. Codex is the unique sustained-active carrier; goose, opencode, gemini-cli, qwen-code, litellm exhibit Tick-N+1 silence after their respective emission peaks at >70% rate within the W17 corpus.

**Evidence at Add.162:**
- **Codex** Add.161 emission peak (4 merges across 4 distinct authors etraut-openai+joeytrasatti-openai+pakrym-oai+friel-openai) → Add.162 sustained-active (2 merges across 2 fully-rotated authors xl-openai+won-openai). Tick-N+1 active continuation: ✓.
- **Goose** Add.161 emission peak (5 merges across 5 distinct authors) → Add.162 silence (0 merges, depth 42m31s post-morgmart #8896 `5fcdf31` 18:28:07Z). Tick-N+1 silence: ✓.
- **Opencode** Add.161 dormancy-exit peak (1 merge rekram1-node #24992 `6aa8e89` 18:15:44Z; structurally a "peak" because it's a hard-deep-dormancy break, not a steady-state emission) → Add.162 silence (0 merges, depth 54m54s). Tick-N+1 silence: ✓.
- **Gemini-cli** Add.161 emission (1 merge Adib234 #26194 `3bc56d0`) → Add.162 active (1 merge gundermanc #26207 `6dec6720`). Tick-N+1 active continuation: ✓ — but this is a 1→1 carry, not a peak→continuation pair (Add.161 was not a "peak" emission for gemini-cli).
- Aggregate: of 3 repos with Add.161 peaks, 1 (codex) sustained active at Add.162 and 2 (goose, opencode) went silent. Asymmetry rate at single-window evidence: 33% sustained vs 67% silent at Tick N+1 — but the 1-of-3 sustained-active repo is **codex**, the unique sustained-keystone repo. Removing codex from the denominator: 0-of-2 non-codex peak-emitters sustain active at Tick N+1. The codex/non-codex split is **complete** in this single-window test.

**Cross-window consistency check:**
- Add.158 codex 1-merge → Add.159 codex active (sustained).
- Add.159 codex 1-merge → Add.160 codex active (sustained).
- Add.160 codex 2-merge → Add.161 codex active (sustained, +2 merges).
- Add.161 codex 4-merge → Add.162 codex active (sustained, 2 merges).
- Codex sustains 6-of-6 active across Add.157-162 (P-161.I/P-352.E confirmed).
- Add.160 goose 1-merge (post-#8884 blog-exit) → Add.161 goose 5-merge BURST (NOT silent). M-161.Y instance.
- Add.161 goose 5-merge → Add.162 goose silent (depth 42m31s). Tick-N+1 silent: ✓.
- Add.161 opencode 1-merge (post-#24992 chore-exit) → Add.162 opencode silent. Tick-N+1 silent: ✓.
- The pattern: codex sustains, opencode-class goes silent, goose oscillates (silent → 5-burst → silent in Add.160→Add.161→Add.162). Goose is **bimodal** in queue-depth: capable of M-161.Y burst when queue is deep but reverts to silence when queue exhausts.

**Status of M-162.A:** First-instance n=1 with internal cross-window consistency checks supporting the codex/non-codex asymmetry. Codex 6-of-6 keystone is the strongest single observation supporting M-162.A.

## New mechanism — M-162.B: codex serialized-series single-tick stall as series-cadence non-monotonicity

**Statement:** A codex serialized series (e.g., the etraut-openai TUI [N/7]) does NOT advance monotonically across consecutive ticks. Within an active series, single-tick stalls (no advance at Tick N+k) can occur even when codex itself is active at Tick N+k. Series advance is **conditional on the specific series-author emitting**, not on codex-aggregate activity. Codex aggregate keystone (6-of-6) is independent from per-series progression.

**Evidence at Add.162:**
- TUI [N/7] series sequence: Add.158 [1/7] #20172 `1c420a90` etraut-openai → Add.160 [2/7] #20173 `44562981` etraut-openai → Add.161 [3/7] #20174 `d0204c3c` etraut-openai → **Add.162 [3/7] STALL** (etraut-openai zero emissions, codex aggregate 2 merges via xl-openai + won-openai on disjoint surfaces).
- Inter-tick advance gap: Add.158→Add.160 = 1 silence-tick gap, Add.160→Add.161 = 0 gap (consecutive), Add.161→Add.162 = 1 stall-tick gap (despite codex being active).
- The "cadence acceleration" frame from Add.161 (24m intra-tick advance) does NOT persist; it was a single-instance phenomenon. Series advance cadence is **non-monotonic** even within an active codex regime.

**Cross-series consistency check:** No other codex serialized series in W17 has explicit `[N/M]` titling in the Add.157-162 window for direct comparison. Series-progression inference relies on the etraut-openai TUI [N/7] alone.

**Status of M-162.B:** Single-series n=1 with one stall instance — claim is structurally derivable but not statistically supported across multiple series.

## Predictions

- **P-353.A** (M-162.A codex/non-codex asymmetry sustains at Add.163): codex Add.163 has ≥1 merge (sustains 7-of-7 active-set) AND at least one of {goose, opencode, gemini-cli, litellm, qwen-code} has Tick-N+1 silence after a Tick-N emission. Falsifier = codex 0 merges at Add.163 OR all non-codex Tick-N-active repos sustain active at Add.163.
- **P-353.B** (M-161.Y reframing — repo-conditional via M-162.A queue-depth dimension): the next 3 M-152.U class exits split as: queue-deep repos (goose, gemini-cli) → M-161.Y burst at Tick N+1 OR N+2 at >50%; queue-shallow repos (opencode, qwen-code) → M-161.Y null at Tick N+1 AND N+2 at >50%. Falsifier = both classes show similar burst/null rates (within 20%) across next 3 exits each.
- **P-353.C** (codex serialized-series stall is single-tick not multi-tick): TUI [N/7] series resumes advance at Add.163 OR Add.164 (etraut-openai #20175 `[4/7]` emerges within 2 ticks of Add.162 stall). Falsifier = etraut-openai #20175 not present in codex Add.163 AND Add.164 emissions.
- **P-353.D** (codex 7-of-7 keystone — extending the W17 longest active-set streak): codex Add.163 ≥1 merge. Falsifier = codex 0 merges at Add.163 (would break the W17 sustained-keystone record at 6-of-6).
- **P-353.E** (cross-repo co-rebound dual reframing — single-tick-only): the next M-152.U class exit produces a Tick-N rate spike ≥1.5× rolling mean at the exit tick BUT Tick-N+1 rate reverts to ≤1.1× rolling mean. Falsifier = Tick-N+1 rate at next exit sustains ≥1.3× rolling mean.

## Summary of evaluations

| Pred ID | Source | Status |
|---|---|---|
| P-352.A | Synth #352 | FALSIFIED at second-instance test |
| P-352.B | Synth #352 | PARTIALLY-TESTED (necessary condition met, awaiting Add.163) |
| P-352.C | Synth #352 | NOT-YET-TESTABLE |
| P-352.D | Synth #352 | NOT-YET-RESOLVED (50% window depleted) |
| P-352.E | Synth #352 | CONFIRMED |
| P-350.E | Synth #350 | REFINED — dual-shape is single-tick-only |
| New: P-353.A | Synth #353 | OPEN |
| New: P-353.B | Synth #353 | OPEN |
| New: P-353.C | Synth #353 | OPEN |
| New: P-353.D | Synth #353 | OPEN |
| New: P-353.E | Synth #353 | OPEN |

## Status

OPEN — P-353.A through P-353.E carried forward to Add.163+. M-162.A at first-instance n=1 with strong codex/non-codex asymmetry support. M-162.B at single-series n=1 awaiting cross-series validation. M-161.Y reframed from "class invariant" to "repo-conditional shape" pending P-353.B resolution.
