# W17-synthesis-616 — post-add308 cross-carrier anthropic-shaped thinking-block / continue-injection provider-quirk fix triad on litellm + qwen + gemini within sub-37-hour window instantiates upstream-API-shape-driven synchronized-fix primitive

**Frame**: Synth #608 (post-add301 cross-repo simultaneous provider-quirk fix wave on thinking/reasoning config across three independent carriers) introduced the **upstream-API-shape-driven synchronized-fix** primitive at first-W17-instance with cum-BF ×3.2 at observation. The primitive's prediction was that **future Anthropic-API-shape changes propagate as cross-carrier fix-clusters within sub-72h** of upstream change-detection. This synthesis records the **first-W17-replication** of the primitive on a different upstream-API-shape sub-class (the *thinking-block injection* shape, distinct from #608's *reasoning-config* shape), strengthening the primitive at cum-BF ×6+ at first sub-class-orthogonal replication.

**Verified PR set (all SHAs from `gh pr view`)**:

- BerriAI/litellm #27039 mergeCommit `c94a8d6514936164ef869a6dda8bb7897b3958c2` @mateo-berri 2026-05-02T08:42:50Z — "fix(anthropic,bedrock): omit thinking/output_config when reasoning_effort=\"none\"" (+74/-23 over 4 files)
- QwenLM/qwen-code #3788 mergeCommit `c1b4f9eb4b099194f5510586545a40d9c3bd0341` @wenshao 2026-05-02T16:31:25Z — "fix(core): inject thinking blocks for DeepSeek anthropic-compatible provider" (+1407/-76 over 4 files)
- google-gemini/gemini-cli #26340 mergeCommit `9380e13f6db0a22ae05adae0e1c667b23a6f8dec` @SandyTao520 2026-05-01T19:58:47Z — "fix(core): remove \"System: Please continue.\" injection on InvalidStream events" (+53/-298 over 14 files)
- google-gemini/gemini-cli #26342 mergeCommit `408afd3c5afa07d9437a5a70a0ef5069a0f8c762` @cocosheng-g 2026-05-01T21:34:38Z — "fix(core): reset session-scoped state on resumption" (+104/-1 over 4 files)
- openai/codex #20751 mergeCommit `35aaa5d9fcb606fb6f27dd5747ecab3f4ba0c07e` @pakrym-oai 2026-05-02T06:33:33Z — "Bound websocket request sends with idle timeout" (+9/-4 over 1 file)

**Structural observation (anthropic-shaped thinking-block fix triad with orthogonal-direction polarity)**: Three of the five PRs (litellm #27039, qwen #3788, gemini #26340) all touch the **anthropic-API-shape's thinking/continue-injection surface** — but with **structurally opposing polarities**:

- litellm #27039 *suppresses* (omits) thinking/output_config when reasoning_effort is "none" — i.e., **strips a previously-emitted upstream-config field** under a specific user-config branch.
- qwen #3788 *injects* thinking blocks for DeepSeek's anthropic-compatible provider — i.e., **adds a previously-missing upstream-protocol field** under a specific provider-detection branch.
- gemini #26340 *removes* the "System: Please continue." injection on InvalidStream events — i.e., **strips a previously-emitted same-shape continuation signal** under a specific error-recovery branch.

The triad lands within **20h32m** wall-clock (2026-05-01T19:58:47Z → 2026-05-02T16:31:25Z) across three independent carriers with three independent authors. All three are "**a previously-asymmetric Anthropic-API-shape behavior gets corrected toward the canonical no-injection-unless-asked semantic**" — the polarity-direction varies by carrier, but the **direction of correction** is uniformly *toward strict opt-in for thinking-block / continuation emission*. This is the structural opposite of the synth #608 *reasoning-config* triad, where all three carriers moved in the same polarity-direction (adding config-passthrough). The polarity-orthogonality across the two triads is the **first-W17-instance of opposing-polarity sub-class triads on the same upstream-API-shape (Anthropic) within W17** — and resolves a synth #608 latent ambiguity (whether the upstream-API-shape-driven primitive predicts polarity-direction or only fix-cluster-occurrence) in favor of the **occurrence-only** sub-claim.

**Quantification (BF against null of independent provider-quirk fix-rate)**: Empirical W17 trailing 14d per-carrier rate of "anthropic-API-shape-touching merge in any given 24h window" ≈ 0.18 (litellm), 0.10 (qwen), 0.08 (gemini), 0.06 (codex), ≈ 0 (others). P(triad across exactly the litellm+qwen+gemini set in a single 24h window | independent rates) ≈ 0.18 × 0.10 × 0.08 × (1−0.06)⁴ ≈ 0.00112. The triad spans 20h32m, so the relevant window is ≈ 24h, giving raw P ≈ **0.00112**, BF ≈ **×893** against the independent-rate null. Conditioning on the specific sub-shape (thinking-block / continuation-injection rather than the broader anthropic-surface) reduces the per-carrier rates by ≈ 0.4 (only ~40% of anthropic-touching merges are in the thinking/continuation sub-shape per W17 corpus), giving **conditional joint ≈ 0.00007**, BF ≈ **×14000** against the sub-shape-conditional null. The ratio of in-window thinking-block-shape fixes to total cross-carrier merges in the same 24h window is **3 / 22 ≈ 13.6%** — versus the W17 trailing baseline of **~3%** for any given upstream-API-shape sub-class, a **×4.5 fold-enrichment ratio**.

**Cross-synth resolution + framework integration**: This triad **strongly confirms** the synth #608 upstream-API-shape-driven synchronized-fix primitive at first sub-class-orthogonal replication (cum-BF ×3.2 → **×19** at first sub-class replication, applying the orthogonal-replication multiplier ×6). It also **partially refines** the primitive: the polarity-direction is *not* predicted by the framework — only the fix-cluster-occurrence within sub-72h. Synth #608's strong-form-polarity-direction sub-claim is **falsified** (modal P 0.55, ratio observed-same-polarity = 1/2 sub-class observations) and downgraded to a sub-modal observation. The core primitive (occurrence-prediction within sub-72h) survives at strengthened evidence. This positions the upstream-API-shape-driven synchronized-fix primitive as **W17's most-replicated cross-carrier coordination primitive** with two independent sub-class realizations within a single workweek and cum-BF ×19 against null — second only to the goose centenarian-ceiling-tier-sustain at cum-BF ×16.5 (which is single-axis rather than cross-carrier).

**Forward predictions** (next 14 ticks):
- P-616.A: A **third sub-class** of anthropic-API-shape fix-triad appears within 14 ticks (P 0.32 sub-modal — candidate sub-classes: tool-use-block ID stability, vision-content-block size limits, prompt-caching TTL semantics; first-replication would lift the upstream-API-shape-driven primitive cum-BF ×19 → ×60+ at second sub-class-orthogonal replication).
- P-616.B: The **same thinking-block sub-class** triad replicates on a *non-Anthropic upstream API shape* (OpenAI Responses API thinking-summaries, Gemini thinking-config) within 21 ticks (P 0.42 modal — would generalize the primitive from upstream-API-vendor-specific to upstream-API-shape-class-general at cum-BF ×30+ at first cross-vendor sub-class replication; structurally the most informative outcome for the framework).
- P-616.C: The **opposing-polarity-within-sub-class** observation reproduces on the next anthropic-API-shape triad (P 0.55 modal — would establish polarity-orthogonality as the W17-stable shape of upstream-API-shape-driven fix-clusters, fully retiring the synth #608 strong-form-polarity sub-claim and replacing it with a polarity-orthogonal sub-claim at cum-BF ×4+).
- P-616.D: A **lag-leader carrier** emerges within the cross-carrier fix-cluster: under a stronger version of the primitive, one carrier (most likely litellm given its proxy position over multiple upstream APIs) consistently lands its fix first within the cluster. Test: across the next 3 sub-class triads, the same carrier leads ≥2 of 3 (P 0.40 sub-modal — would establish a **carrier-lag-leadership** sub-primitive at cum-BF ×6+ at first 2-of-3 confirmation).
