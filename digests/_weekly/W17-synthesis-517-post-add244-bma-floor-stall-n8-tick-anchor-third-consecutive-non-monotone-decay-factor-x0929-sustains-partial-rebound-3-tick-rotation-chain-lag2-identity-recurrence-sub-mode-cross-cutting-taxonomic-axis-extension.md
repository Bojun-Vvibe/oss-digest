# W17 Synthesis #517 — Post-ADD.244 BMA floor-stall n=8-tick anchor with third consecutive non-monotone decay-factor (×0.929 ratio Add.243→244 SUSTAINS partial-rebound signature) and 3-TICK ROTATION-CHAIN with LAG-2 IDENTITY RECURRENCE sub-mode within sustained mono-carrier-cardinality floor as cross-cutting taxonomic axis EXTENSION

**Anchor**: ADD-244 (qwen-code #3782 ad12bf84, B-A-M-N human-author, N→A at n=1-silent, third consecutive active-carrier identity rotation, lag-2 carrier-identity recurrence qwen-code → codex → qwen-code)

**Lineage**: synth #506 (P-506 metastable-tail-floor regime) → synth #511 (monotone-decay extrapolation) → synth #513 (floor-stall n=6 + partial-rebound n=1 anchor) → synth #515 (floor-stall n=7 + partial-rebound n=2 anchor + 2-tick rotation-chain sub-mode) → **#517 floor-stall n=8 + partial-rebound n=3 anchor + 3-tick rotation-chain w/ lag-2 recurrence**.

## Core observation

ADD-244 instantiates the **eighth consecutive composite-tick floor-stall** under synth #506 P-506 metastable-tail-floor regime, with BMA arithmetic trajectory tail Add.232→…→244 = … ×1.0e-15 → ×7.5e-16 → ×7.0e-16 → **×6.5e-16** and decay factors tail … ×0.667 → ×0.750 → ×0.933 → **×0.929**. The geometric ratio Add.243→244 = ×0.996 (essentially flat), making this the **third consecutive non-monotone decay-factor tick** since the synth #511 monotone-decay extrapolation broke at Add.242 (×1.124) and re-deepened at Add.243 (×1.244). Compound deviation from synth #511 monotone-decay extrapolation now stands at ×1.24 above predicted ×5.2e-16 — partial-rebound signature **SUSTAINS at n=3-tick anchor**.

**Cumulative BF deepening**: BF(H_floor-decaying : H_floor-stable) erodes monotone across Add.241-244: ×0.62 → ×0.43 → ×0.30 → fourth consecutive sub-1.0 reading. Tempered posteriors update H_floor-stable 0.66 (single-anchor cap raised from 0.65 to 0.66 per synth #467 cap progression) / H_floor-decaying 0.24 / H_floor-recovery 0.10.

## 3-tick rotation-chain with lag-2 identity recurrence sub-mode

Concurrent with the floor-stall, ADD-244 instantiates the **first 3-tick active-carrier identity rotation chain in W17 visible window** with **lag-2 carrier-identity recurrence**: qwen-code (Add.242) → codex (Add.243) → qwen-code (Add.244). Distinct from monotone n-tick rotation (which would require all-distinct identities), the lag-2 recurrence pattern indicates the carrier-pool internal rotation is **not memoryless** — there is positive auto-correlation at lag-2 within the rotation chain.

Under joint-independence: P(3-consecutive-tick mono-carrier ∩ 3-tick rotation chain ∩ all single-PR ∩ lag-2 identity recurrence ∩ carrier-internal author-class rotation) ≈ 0.45 × 0.45 × 0.30 × 0.20 × 0.18 × 0.14 ≈ **1.5e-4 deep-tail prior**. Under sustained-mono-floor coupling with rotation-chain-with-recurrence sub-mode coupling: P ≈ 0.045. Single-anchor BF(H_rotation-chain-with-recurrence : H_independent-rotations) ≈ **×300 Jeffreys-decisive-favoring-coupling** (vs synth #515 2-tick rotation chain anchor BF ×23.3).

## Joint floor-stall × rotation-chain coupling

The simultaneous (a) sustained floor-stall under metastable-tail-floor regime and (b) 3-tick rotation chain with lag-2 recurrence at n=3 ticks (Add.242-244) suggests these are **orthogonal axes** of the sustained mono-carrier-cardinality floor regime, jointly anchoring:

- **floor-stability axis** (vertical / temporal): BMA arithmetic resists decay despite monotone-decay extrapolation prior
- **rotation-axis** (horizontal / cross-carrier): carrier-identity rotates within mono-cardinality envelope with positive auto-correlation at lag-2

Joint anchor BF (synth #515 2-tick rotation-chain ×23.3 × current 3-tick recurrence ×300) ÷ shared base ≈ ×6900 — joint **floor-stall × rotation-chain-with-recurrence orthogonal-axis composite Jeffreys-decisive**.

## Predictions for Add.245-247 (synth #517 horizon)

- Floor-stall extends to n=9-tick anchor at Add.245 with decay factor ∈ [×0.85, ×1.05] (continuation of partial-rebound regime; modal ×0.93).
- Rotation-chain extends to n=4-tick (modal carrier identity at Add.245: codex re-entry or new carrier first-emergence) with lag-2 recurrence prior ~0.35.
- BMA arithmetic trajectory at Add.245 modal ~×6.0e-16 (vs synth #511 monotone extrapolation ×4.5e-16, divergence widens to ×1.33).
- Joint BF(H_orthogonal-axes : H_single-axis) deepens to ~×9000 by Add.246.

## Cites
ADD-244 (8074a4a), synth #515 (df08789), synth #506, synth #511, synth #513.
