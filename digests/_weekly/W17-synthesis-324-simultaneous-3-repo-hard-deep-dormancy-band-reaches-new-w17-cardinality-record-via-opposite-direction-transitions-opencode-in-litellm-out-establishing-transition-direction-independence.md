# W17 Synthesis #324 — Simultaneous-3-repo HARD-DEEP-DORMANCY BAND ({gemini-cli n=9, qwen-code n=6, opencode n=4} at Add.146) reaches NEW W17 CARDINALITY RECORD via OPPOSITE-DIRECTION TRANSITIONS at the same tick (opencode enters, litellm exits) — falsifies the synth #320 synchronized-class-transition framing at the corpus-cardinality level and establishes per-repo TRANSITION-DIRECTION INDEPENDENCE as the third-axis correlate of intrinsic exit-readiness

**Scope:** Add.146 (2026-04-29T07:18:19Z) hard-deep-dormancy band cardinality state, with full Add.138-146 entry/exit trajectory across all 4 repos that have crossed n=4 (gemini-cli, goose, qwen-code, opencode), and reference to synth #320 P-320.A/B (synchronized class transition), synth #322 three-axis state model (per-repo intrinsic exit-readiness), and synth #313 deep-dormancy class membership growth.

## Claim (falsifiable)

**P-324.A:** At Add.146, the W17 hard-deep-dormancy class (n≥4) reaches **simultaneous cardinality 3** — `{gemini-cli n=9, qwen-code n=6, opencode n=4}` — a **new W17 record** for simultaneous hard-deep-dormancy repo count, surpassing the prior peak of 2 (gemini-cli + goose at Add.144 mid-tick, before goose exited at Add.145; or gemini-cli + qwen-code from Add.144 onward, before opencode joined at Add.146). The cardinality-3 state is reached **specifically via opposite-direction transitions at the same Add tick**: opencode crosses INTO the class (n=3 → n=4), while litellm — which was on the n=3 candidate-soft trajectory predicted by Add.145 P-145.F to **also** cross n=4 jointly — instead **EXITS** the candidate-soft threshold (n=3 → n=0 via Sameerlite #26757 self-merge).

**P-324.B:** The Add.146 opposite-direction transition pair (opencode INTO hard class, litellm OUT of candidate-soft class, in the same tick) is structurally a **second-instance multi-repo class-transition asymmetry**, replicating the Add.145 goose-exit / qwen-code-extend asymmetry described in synth #322 P-322.A. The synth #320 P-320.B prediction of corpus-silence-mediated synchronized transitions is **falsified at a third configuration** in 2 consecutive ticks: at Add.145 the falsification was at the deep-dormancy exit class (goose vs qwen-code), at Add.146 the falsification is at the **threshold-crossing direction** (opencode in vs litellm out). The synth #322 three-axis model is now extended: per-repo intrinsic exit-readiness governs not only **whether** a repo participates in a class transition (synth #322 axis iii) but also **the direction** of that transition (axis iii.a, transition-direction independence).

**P-324.C:** The simultaneous-3-repo hard-deep-dormancy cardinality at Add.146 is **NOT a synth #320-style synchronized state** but rather a **STAGGERED-ENTRY ACCUMULATION** state: each of the 3 hard-deep-dormancy repos entered the class at a distinct Add tick (gemini-cli at Add.138, qwen-code at Add.144, opencode at Add.146 — with goose having entered at Add.144 and exited at Add.145, no longer counted). The simultaneous cardinality grows because the **exit rate is lower than the entry rate** in the late-W17 regime: 4 entries (gemini-cli Add.138, goose Add.144, qwen-code Add.144, opencode Add.146) vs 1 exit (goose Add.145) in the Add.138-146 9-tick band, yielding a net-cardinality-growth rate of +0.33 repos/tick. If this rate continues, the W17 hard-deep-dormancy band reaches cardinality 4 (all 4 distinct entries simultaneously dormant) within Add.147-149.

## Evidence (Add.138-146 hard-deep-dormancy entry/exit ledger across all 4 affected repos)

### Entry/exit timeline (n=4 hard threshold crossings only)

| Tick | Repo | Event | Anchor PR (last merge) | Anchor SHA | Class cardinality after event |
|---|---|---|---|---|---|
| Add.138 | gemini-cli | **ENTRY n=4** | g-samroberts #26150 | `c7d5fcff` | 1 |
| Add.139-143 | gemini-cli | (extends, no event) | — | — | 1 (sustained) |
| Add.144 | gemini-cli | (extends to n=7, sets W17 record) | — | — | 1 |
| Add.144 | goose | **ENTRY n=4** | lifeizhou-ap #8890 | `56b84590` | 2 |
| Add.144 | qwen-code | **ENTRY n=4** | pomelo-nwu #3577 | (per Add.140) | **3** (gemini-cli + goose + qwen-code) |
| Add.145 | gemini-cli | (extends to n=8) | — | — | 3 |
| Add.145 | goose | **EXIT** (n=4 → n=0) via dual-author pair | #8895 lifeizhou-ap `e4ef5ea4` + #8873 matt2e `0bb87dae` | — | 2 (gemini-cli + qwen-code) |
| Add.145 | qwen-code | (extends to n=5) | — | — | 2 |
| Add.146 | gemini-cli | (extends to n=9, 2nd consecutive record extension) | — | — | 2 |
| Add.146 | qwen-code | (extends to n=6) | — | — | 2 |
| Add.146 | **opencode** | **ENTRY n=4** | rekram1-node #24869 | `504ca3d3` | **3** (gemini-cli + qwen-code + opencode) — **NEW W17 RECORD** |

The Add.146 cardinality-3 state is structurally **distinct** from the Add.144 cardinality-3 state in two ways: (i) Add.144 was reached via **simultaneous synchronized entry** (goose + qwen-code crossed n=4 in the same tick), while Add.146 is reached via **single-repo entry** (only opencode crosses); (ii) Add.144 was reached during a **corpus full-silence tick** (rate 0.0000), while Add.146 is reached during a **non-silent tick** (rate 0.0489).

### Opposite-direction transition pair at Add.146 (opencode IN / litellm OUT)

| Repo | Add.145 state | Add.146 transition | Add.146 state | Direction |
|---|---|---|---|---|
| opencode | n=3 (candidate-soft) | crosses n=4 hard threshold | **n=4 (hard deep-dormancy)** | **INTO class** |
| litellm | n=3 (candidate-soft) | exits via Sameerlite #26757 self-merge | **n=0 (active)** | **OUT of candidate-soft** |

Both repos started Add.146 at **n=3 candidate-soft** (per ADDENDUM-145 #7 — "opencode silence extends to n=3 post-#24869; litellm silence extends to n=3 post-#26747"). The Add.145 P-145.F prediction was **joint entry into n=4** (opencode AND litellm both cross). The observed outcome is **opposite directions**: opencode crosses INTO n=4, litellm crosses OUT of n=3. The transition-direction divergence between two repos with **identical pre-tick state** is the strongest possible falsification of synth #320 P-320.B's synchronized-class-transition framing.

### Exit/entry rate ledger (Add.138-146 9-tick band)

| Event class | Count | Repos affected | Rate (events/tick) |
|---|---|---|---|
| Hard-deep-dormancy ENTRY | 4 | gemini-cli (Add.138), goose (Add.144), qwen-code (Add.144), opencode (Add.146) | 0.444 |
| Hard-deep-dormancy EXIT | 1 | goose (Add.145) | 0.111 |
| **NET cardinality change** | **+3** | — | **+0.333** |

At the current entry-exit rate asymmetry (4 entries vs 1 exit over 9 ticks), the net-cardinality-growth rate is **+0.333 repos/tick**. The cardinality-4 state (all 4 entered repos simultaneously hard-dormant) requires either qwen-code OR gemini-cli to remain dormant AND opencode to remain dormant AND a 4th repo to enter. The 4th entry candidate is **goose itself** (re-entered silence at Add.146 n=1, on trajectory back to n=4 if 3 more silence ticks accumulate) or **codex** (currently active but with the burst-then-silence rhythm of Add.140-146; if Add.147+ silences chain, codex could approach n=4 by Add.151+).

## Differentiation from prior synths

- **vs synth #313 (deep-dormancy class membership growth):** synth #313 documented the entry of gemini-cli as the **3rd** repo into deep-dormancy. Synth #324 documents the simultaneous (rather than cumulative) cardinality reaching **3** at Add.146, which is a distinct measure (membership ever vs membership simultaneously). The W17 cumulative deep-dormancy-class membership is now **{gemini-cli, goose, qwen-code, opencode}** (4 distinct repos have ever crossed n=4); the W17 simultaneous deep-dormancy cardinality at Add.146 is **3** (gemini-cli + qwen-code + opencode — goose has exited).
- **vs synth #316 (M-312-M non-absorbing rule):** synth #316 confirmed deep-dormancy is non-absorbing. Synth #324 quantifies the entry/exit asymmetry: 4 entries vs 1 exit in 9 ticks → entry rate is 4× exit rate, sustaining net cardinality growth despite non-absorbing class.
- **vs synth #320 (corpus-silence-mediated synchronized transition):** synth #320 P-320.B predicted that corpus-silence regime mediates synchronized multi-repo class transitions. Synth #322 falsified the **exit** symmetry (goose vs qwen-code at Add.145). Synth #324 now falsifies the **direction** symmetry (opencode IN vs litellm OUT at Add.146) — a strictly stronger falsification because Add.146 is **not** a corpus-silence tick (rate 0.0489), so the synth #320 mediator (corpus-silence regime) does not even apply.
- **vs synth #322 (per-repo intrinsic exit-readiness three-axis model):** synth #322 introduced axis (iii) per-repo intrinsic exit-readiness. Synth #324 extends to axis (iii.a) **per-repo transition-direction independence**: even when two repos have identical pre-tick state and face the same corpus regime, their next-tick transition direction can be opposite (opencode IN, litellm OUT at Add.146). The intrinsic exit-readiness axis is therefore vector-valued (direction + magnitude), not scalar.

## Falsifiers (P-324.A / P-324.B / P-324.C)

- **P-324.A falsifier:** any Add.147 merge at gemini-cli OR qwen-code OR opencode would reduce simultaneous hard-deep-dormancy cardinality from 3 to 2, reverting the new W17 record within 1 tick. (Most likely candidate: opencode, which has the lowest n value at 4 and the most recent entry, may have the highest exit-readiness.)
- **P-324.B falsifier:** any Add.147 multi-repo class transition where ≥2 repos move in the **same direction** with **same pre-tick state** would partially restore the synth #320 P-320.B synchronized-direction framing. Specifically: if both gemini-cli AND qwen-code emit merges at Add.147, that's a synchronized exit at the hard-deep-dormancy class; if neither does AND opencode also stays silent, that's synchronized extension.
- **P-324.C falsifier:** any Add.147-149 sequence where the entry/exit rate asymmetry **inverts** (more exits than entries) would falsify the staggered-entry-accumulation framing and suggest the late-W17 deep-dormancy cardinality is **near a peak** rather than monotonically growing. Specifically: if 2+ of `{gemini-cli, qwen-code, opencode}` exit at Add.147, the cardinality drops to ≤1, falsifying the +0.333 growth rate as a stable feature.

## Top-level commits to track Add.147+

- The **3-repo simultaneous hard-deep-dormancy band** is the most fragile structure observed in W17 to date (any of 3 single-repo merges drops it). Tracking which repo exits first (if any) at Add.147 will discriminate between the synth #322 per-repo exit-readiness axis and the synth #320 corpus-mediated framing.
- If P-324.A AND P-324.B both hold at Add.147 (simultaneous cardinality stays ≥3, no synchronized direction observed), the synth #320 P-320.B framing is **conclusively dead** at the hard-deep-dormancy class level, requiring full replacement by the synth #322 + synth #324 per-repo independence model.
- The codex Add.147 burst-extension prediction from synth #323 P-323.A interacts with synth #324: if codex stays active at Add.147 (per synth #323) AND the 3-repo hard-dormancy band stays intact (per synth #324), the W17 corpus reaches a maximally-bifurcated state — 3 repos in hard-dormancy + 1 active anchor repo (codex) + remaining 2 repos in mixed states.

## Citations

- ADDENDUM-146.md (sha=cd98f83): opencode n=4 entry, litellm n=3 → n=0 exit, simultaneous 3-repo cardinality, opposite-direction transition pair, P-146.C origin
- ADDENDUM-145.md (sha=0e19f9d): goose n=4 → n=0 exit, qwen-code n=4 → n=5 extension, opencode + litellm n=3 candidate-soft state, P-145.F joint-entry prediction (now twice falsified)
- ADDENDUM-144.md (sha=3a7d986): goose + qwen-code joint n=4 entry, gemini-cli n=7 W17 record
- ADDENDUM-138 (referenced via Add.140-145 chain): gemini-cli initial n=4 entry, deep-dormancy class founding event
- W17-synthesis-313: deep-dormancy 3-repo membership cumulative growth
- W17-synthesis-316: M-312-M non-absorbing rule
- W17-synthesis-320: corpus-silence-mediated synchronized class transition (now falsified at exit-symmetry per #322 and direction-symmetry per #324)
- W17-synthesis-322 (sha=2ea2616): per-repo intrinsic exit-readiness three-axis model (extended by #324 to vector-valued axis iii.a transition-direction independence)
- W17-synthesis-323 (sha=a5b85a7): codex Add.145+146 stacked-PR-series same-author cross-event coherence (parallel pattern at burst class vs synth #324's class-transition direction independence)
