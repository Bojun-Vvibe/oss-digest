# W17 Synthesis #416 — Single-Author Batch-Merge Motif at Canonical Instantiation: Sub-Tick Inter-Arrival Sub-Process Distinct From Cross-Tick Distribution at Opencode kitlangton 4-PR / 3m03s Span Add.193

**Trigger**: ADDENDUM-193 sha=`ef4d530` M-193.F — opencode quadruplet discharge at Add.193 instantiated as **single-author batch-merge motif** with kitlangton (recurrent author per synth #410 fit-class) merging 4 thematically-coherent test-suite refactor PRs (#25049, #25115, #25052, #25050) within a 3m03s mergedAt window. This is the **canonical instantiation** of the batch-merge sub-mode that explains heavy-tail upper readings of single-emit-tick amplitude (amplitude ≥3 at single-carrier regimes).

## Lineage and prior framework

Prior W17 synth lineage on amplitude-distribution and per-tick emission profile:

- **Synth #410** (sha=`759c7fd`): Per-repo silence-horizon fit-class divergence; six distinct recovery laws under single cohort regime; recurrent-author class introduced as fit-class refinement at per-repo amplitude-trajectory.
- **Synth #411** (sha=`20cad94`): Post-rupture cohort-amplitude geometric-decay law `5 → 1 → 0` confirmed at n=2; the geometric-decay applies at the **cohort-amplitude** (cross-repo aggregate) dimension, not the per-repo-per-author dimension.
- **Synth #412** (sha=`a5e5a1e`): Fit-class entropy bifurcation; H_emitting bimodal {0, 1.000} bits regime; the per-tick H_emitting reading depends on the active-emitting fit-class set cardinality and probability mass.

The implicit framework across #410 → #412 lineage assumed the per-tick amplitude was generated by a **uniform-rate emission process** at the per-repo dimension — i.e., each repo had a per-tick amplitude drawn from a repo-specific distribution, and the cohort-amplitude was the sum. Add.193 falsifies this framework at the single-author batch-merge instance: the four kitlangton PRs merged within 3m03s constitute a **sub-tick inter-arrival sub-process** with mean inter-merge gap 1m01s, which is **two orders of magnitude shorter** than the typical cross-tick inter-arrival gap (~30-90 minutes per tick width).

## The Add.193 single-author batch-merge event

Detailed timing (UTC):
- PR #25049 mergedAt 2026-04-30T16:52:30Z (kitlangton, "test: use Effect test helper for app runtime logger")
- PR #25115 mergedAt 2026-04-30T16:53:13Z (kitlangton, "test: use testEffect for instance state") — Δ=43s from #25049
- PR #25052 mergedAt 2026-04-30T16:54:53Z (kitlangton, "test: use testEffect for plugin workspace adaptor") — Δ=100s from #25115
- PR #25050 mergedAt 2026-04-30T16:55:33Z (kitlangton, "test: use testEffect for retry policy") — Δ=40s from #25052

Inter-merge gaps: {43s, 100s, 40s}. Mean 61s = 1m01s. Min 40s. Max 100s. Standard deviation ≈ 26.5s. Coefficient-of-variation ≈ 0.43. The four PRs span 3m03s in mergedAt time, contributed by a single recurrent author, are thematically coherent (testEffect helper migration), and constitute 100% of the in-window cohort amplitude at Add.193.

## The two-scale inter-arrival distribution

The W17 inter-arrival distribution at single-PR-merge events admits **two distinct scales**:

1. **Cross-tick inter-arrival scale** (~30-90 minute width per tick; the "tick width" axis): the dominant scale at the cohort-aggregate level; characterizes the cross-tick rate at which new emit-events appear.
2. **Sub-tick inter-arrival scale** (~30-200 second mean within batches; the "batch span" axis): a sub-process active only at single-author batch-merge events; characterizes the within-batch merge cadence.

The two scales differ by a factor of ~30-150x in mean inter-arrival, and the sub-tick scale appears to be an **author-driven push-event scale** (single author pushing multiple thematically-coherent PRs to the queue, with merges occurring as the author processes the queue serially).

## Invariant candidate I-416.A: Single-author batch-merge sub-tick scale

Define I-416.A: at any observed single-author batch-merge event B (≥3 PRs by single author within a single tick), the within-batch inter-merge gap distribution admits mean ≤ 5 minutes (300s) and max ≤ 10 minutes (600s) at single-anchor n=1 supporting.

- **Empirical at n=1** (Add.193 kitlangton 4-PR batch): mean 61s, max 100s — well within the proposed bounds.
- **F-416.A falsification rule**: I-416.A falsifies if next observed single-author batch-merge event admits mean > 5 minutes OR max > 10 minutes. Single counter-example terminates the bound at single-anchor n=2.

## Invariant candidate I-416.B: Single-author batch-merge thematic-coherence requirement

Define I-416.B: at any observed single-author batch-merge event B (≥3 PRs by single author within ≤5 min span), the PR titles/themes admit **thematic coherence** — i.e., the PRs share a common refactor theme, feature area, or migration target.

- **Empirical at n=1** (Add.193 kitlangton 4-PR batch): all four PR titles begin with "test: use testEffect/Effect test helper" — testEffect helper migration is a single coherent refactor theme. Thematic coherence confirmed at single-anchor n=1 supporting.
- **F-416.B falsification rule**: I-416.B falsifies if next observed batch admits incoherent themes (e.g., one PR is a feature, another a docs change, another a CI fix, no shared migration target). Single counter-example terminates the coherence prior at single-anchor n=2.

## Invariant candidate I-416.C: Heavy-tail upper-amplitude single-tick readings are batch-merge-mediated

Define I-416.C: at any observed single-tick cohort-amplitude reading ≥ 3 by a single carrier-repo, the dominant contribution is a **single-author batch-merge** (i.e., ≥75% of the amplitude is contributed by a single recurrent author).

- **Empirical at n=1** (Add.193 opencode amplitude=4): kitlangton contributes 4/4 = 100% of the amplitude. Confirms at single-anchor n=1 supporting.
- **Retrospective check**: Add.169 codex amplitude=5 (post-Add.168 sextuple) — needs author-set check; deferred to W17 retrospective audit. Add.171-173 codex profile `5 → 4 → 6` — needs author-set check.
- **F-416.C falsification rule**: I-416.C falsifies if next observed single-carrier amplitude ≥ 3 reading admits multi-author distribution with no single-author dominance ≥ 75%. Single counter-example reframes the heavy-tail mechanism from author-driven-batch to multi-author-coincident-merge.

## Refined amplitude-distribution model

The synth #411 cohort-amplitude geometric-decay law applies at the cross-tick scale; synth #416 introduces a **multiplicative sub-process** at single-tick single-carrier amplitude:

cohort-amplitude(T) = Σ_repo single-carrier-amplitude(T, repo)

single-carrier-amplitude(T, repo) = Σ_author within-tick-author-amplitude(T, repo, author)

where within-tick-author-amplitude follows the **sub-tick inter-arrival sub-process** at single-author batch-merge events (mean batch size ≥3 at I-416.C qualifying instances; mean within-batch inter-arrival ~1 minute at I-416.A bound).

This refined model decomposes the amplitude into three axes: cross-tick rate (cohort-aggregate), per-repo carrier-selection (multinomial over active repos), and per-author batch-amplitude (heavy-tail at single-author push events). The third axis explains the heavy-tail upper readings without requiring high-variance generative noise at the cohort-aggregate dimension.

## Predictions (P-416.X)

- **P-416.A**: Add.194 next opencode emission window (whenever it occurs) admits batch-merge motif recurrence at >40% (kitlangton recurrent-author class active; testEffect refactor theme suggests multi-PR migration in progress; remaining batch PRs likely in opencode pending queue). Within-batch mean inter-merge gap ≤ 5 minutes at >85% conditional on batch-occurrence.
- **P-416.B**: Next W17 single-tick single-carrier amplitude ≥ 3 reading admits single-author dominance ≥ 75% at >70% (I-416.C invariant at single-anchor n=1 supporting; needs n=3 to discriminate from sample-noise).
- **P-416.C**: Next observed single-author batch-merge event admits thematic coherence at >65% (I-416.B at single-anchor n=1; refactor / feature-batch / migration themes are dominant batch-types per general OSS PR-batching observation).
- **P-416.D**: Cross-tick inter-arrival mean across W17 next 10 ticks (Add.194-203) sustains in [30m, 60m] band at >55% (P-193.H band-stickiness 9/10; sub-tick sub-process is orthogonal to cross-tick scale and does not perturb the tick-width distribution).
- **P-416.E**: H_emitting at Add.194 single-carrier emit-tick (if observed) sustains 0-bit reading at >65% (single recurrent-author batch is single-class single-carrier; H_emitting=0; bimodal {0, 1.000}-bits prior holds; transition to multi-class requires multi-author at same tick which is rare at single-anchor).
- **P-416.F**: I-416.A bound (mean ≤ 5min, max ≤ 10min within-batch inter-merge gap) holds at next observed batch-merge event at >75% (single-anchor n=1 supporting; bound is generous given Add.193 observed mean=1m01s).
- **P-416.G**: Retrospective audit of W17 prior single-carrier amplitude ≥ 3 readings (Add.169 codex=5, Add.171 codex=5, Add.173 codex=6, Add.180 opencode=2-doublet not qualifying at amplitude≥3) confirms I-416.C single-author dominance at ≥75% in ≥3 of ≥4 retrospective instances at >60% prior (deferred to W17 retrospective audit at next dispatcher tick).

## Falsification rules

- **F-416.1**: I-416.A falsifies if next observed single-author batch-merge event admits within-batch mean inter-merge gap > 5 minutes or max > 10 minutes.
- **F-416.2**: I-416.B falsifies if next observed batch admits incoherent themes across constituent PRs (no shared refactor / feature / migration target).
- **F-416.3**: I-416.C falsifies if next observed single-carrier amplitude ≥ 3 reading admits multi-author distribution with no single-author ≥ 75% contribution.
- **F-416.4**: P-416.B falsifies at sample-n ≥ 3 of single-carrier amplitude ≥ 3 readings if cumulative single-author-dominance fraction < 0.50 across the sample.

## Anchor citations

- **ADDENDUM-193** sha=`ef4d530` (M-193.A opencode quadruplet discharge; M-193.F single-author batch-merge motif at canonical instantiation; per-merge inter-arrival sub-second-scale at 1m01s mean).
- **ADDENDUM-192** sha=`f75a52c` (M-192.B codex H=7 discharge at amplitude=1; carrier-rotation pre-condition).
- **W17 synth #415** sha=`5392b01` (post-discharge transition tri-modal distribution; carrier-rotation as distinct mode; partner synth in current dispatcher tick).
- **W17 synth #414** sha=`db7140f` (codex right-censored geometric discharge-point validation at single-PR amplitude=1; contrasts Add.193 amplitude=4 batch).
- **W17 synth #413** sha=`b89f50c` (cohort-zero sojourn-distribution bimodal-or-heavy-tail revision; complementary at the cohort-zero-block-distribution dimension).
- **W17 synth #412** sha=`a5e5a1e` (fit-class entropy bifurcation; H_emitting bimodal {0, 1.000} bits regime; Add.193 H_emitting=0 confirms at second consecutive tick supporting).
- **W17 synth #411** sha=`20cad94` (post-rupture cohort-amplitude geometric-decay; refined here at the per-author sub-tick decomposition).
- **W17 synth #410** sha=`759c7fd` (per-repo silence-horizon fit-class divergence; recurrent-author class definition; foundational for I-416.C single-author dominance gating).
- **W17 synth #409** sha=`4764146` (codex H-fit terminally invalidated; right-censored geometric reframe; distal lineage anchor).

## Disposition

Synth #416 **promotes** the single-author batch-merge motif from "novel observation at Add.193 M-193.F" to "canonical sub-mode at single-anchor n=1 supporting"; **introduces** a refined three-axis amplitude-decomposition model (cross-tick rate / per-repo carrier-selection / per-author batch-amplitude) that explains heavy-tail upper amplitude readings via author-driven push events rather than high-variance generative noise; **promotes** I-416.A (within-batch inter-merge gap bound), I-416.B (thematic coherence requirement), I-416.C (single-author dominance gating heavy-tail amplitudes) as candidate invariants at single-anchor n=1 supporting; **calls for** W17 retrospective audit of prior single-carrier amplitude ≥ 3 readings (Add.169, Add.171, Add.173) to validate I-416.C at sample-n ≥ 3 supporting; **complements** synth #415 carrier-rotation framework — the carrier-rotation event at Add.192-193 boundary is co-instantiated with the batch-merge motif at the destination-carrier (opencode kitlangton) but is not structurally required by it; the two synth-416 invariants apply orthogonally at single-author batch-merge events regardless of the upstream cohort-state.
