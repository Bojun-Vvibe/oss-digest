# W17 Synthesis #534 — post-Add.252 ANCHOR-MERGE TWO-TICK SUSTAIN AT LITELLM #27039 (`c94a8d65`) INSTANTIATES FIRST CROSS-REPO PRIOR-MERGE-PERSISTENCE OBSERVATION + ALL-SILENT CLUSTER HYPOTHESIS PROMOTED TO MAJORITY-PLURALITY VS PERIODIC-3TICK DISCRIMINATOR + ZERO-SUSTAIN SUB-MODE PROMOTED TO SUBSTANTIAL-TIER VIA LOW-ZERO MARKOV CYCLE FALSIFICATION-DRIVEN POSTERIOR MASS REDISTRIBUTION

A second-order analytical move on the Add.252 zero-class triplet on a structurally orthogonal angle from synth #533: the **same anchor merge** (litellm #27039 sha=`c94a8d65` mateo-berri 2026-05-02T08:42:50Z) now persists as the last-merged cross-watched event across **TWO consecutive zero-class ticks** (Add.251 + Add.252 = 41m13s + 57m47s = 99m of post-merge silence). This is the **first sustained-anchor pair in W17 visible window** and instantiates a candidate **cross-repo prior-merge-persistence sub-mode** distinct from in-window merge-rate analysis. Concurrently, the all-silent cluster hypothesis (1-tick consecutive) at Add.251+Add.252 is **promoted to majority-plurality** vs periodic-3tick (which would have predicted next all-silent at Add.254, NOT Add.252). And the zero-sustain sub-mode (synth #532 candidate at 0.05 prior) is **promoted to substantial-tier (0.20)** via low-zero Markov cycle falsification-driven posterior mass redistribution at Add.252 single-tick discriminator.

## Anchor

**Source**: ADD-252 (2026-05-02), window 10:10:19Z → 10:51:32Z (41m13s).
**Anchor merge**: litellm #27039 sha=`c94a8d65` mateo-berri 08:42:50Z — sustains as last cross-watched merge across Add.251 + Add.252 (99-min post-merge silence chain).
**All-silent cluster**: 1-tick consecutive 7-of-7 silent at Add.251+Add.252 (PJL=36 → PJL=37).
**Posterior redistribution**: low-zero Markov cycle 0.45 → 0.18, zero-sustain sub-mode 0.05 → 0.20.

## Anchor-merge two-tick sustain at litellm #27039

The Add.251 ADDENDUM noted "last cross-watched merge prior to window-open was litellm #27039 sha=`c94a8d65` mateo-berri 08:42:50Z". Add.252 ADDENDUM notes the **identical anchor merge sustains** as the last cross-watched event prior to its window open. This 2-tick anchor-sustain is structurally distinct from the prior W17 visible-window pattern where each zero-class tick had a different (more recent) anchor merge.

**Anchor-merge persistence sequence Add.232-252 tail**:
… Add.247 anchor: litellm #27021 sha=`af3e0c12` mateo-berri 08:11:33Z (low-class tick, 1-tick anchor)
… Add.248 anchor: litellm #27021 sha=`af3e0c12` (zero-class tick, **2-tick anchor sustain — but DIFFERENT carrier identity at active-set level**)
… Add.249 anchor: litellm #27033 sha=`a9014650` (mid-class tick, new anchor at single-tick) — superseded
… Add.250 anchor: litellm #27039 sha=`c94a8d65` (low-class tick, 1-tick anchor)
… Add.251 anchor: litellm #27039 sha=`c94a8d65` (zero-class tick, 2-tick anchor sustain at SAME carrier-identity level — **first 2-tick same-anchor sustain in W17 visible window**)
… **Add.252 anchor: litellm #27039 sha=`c94a8d65` (zero-class tick, 3-tick anchor sustain at SAME carrier-identity level — first 3-tick same-anchor sustain in W17 visible window)**

**Cross-repo prior-merge-persistence sub-mode hypothesis**: under H_persistent-anchor (anchor merge persists across zero-class triplet because no carrier replaces the anchor), the persistence depth scales linearly with zero-class triplet depth. Under H_independent-anchor (anchor would change at any tick by carrier rotation), persistence depth distribution under W17 baseline is geometric with mean ~1.4-tick. Observed 3-tick persistence at Add.250 → Add.251 → Add.252 sits at upper-tail of geometric distribution with P ≈ 0.07 under H_independent. Under H_persistent-anchor conditional on zero-class triplet, P ≈ 0.85.

**Single-cumulative BF(H_persistent-anchor : H_independent-anchor) at 3-tick anchor-sustain**: ×12.1 substantial-tier-favoring-persistent.

**Predicted Add.253 anchor-merge persistence**:
- If Add.253 = zero (P ≈ 0.50 under cluster hypothesis): anchor sustains at 4-tick depth with P ≈ 0.85. Joint event P(zero ∩ 4-tick anchor sustain) ≈ 0.42.
- If Add.253 = low/mid/high: anchor likely changes (P ≈ 0.85 anchor change conditional on non-zero class). Joint event P(non-zero ∩ anchor change) ≈ 0.42.
- Net Add.253 anchor sustain at 4-tick predicted P ≈ 0.42.

**Implication for cross-repo trend**: the persistent-anchor sub-mode is a **secondary observable** of the all-silent cluster — they co-instantiate. If all-silent cluster sustains, anchor sustains. Joint event posterior at synth #534 first-anchor: H_joint-cluster-anchor 0.45 / H_independent-channels 0.35 / H_alt 0.20.

## All-silent cluster hypothesis promoted to majority-plurality

Synth #532 instantiated periodic all-silent cycle candidate with hypothesised period ~3-4 ticks (Add.248 → Add.251 = 3-tick gap). Add.252 instantiates all-silent at **1-tick gap** from Add.251 (NOT at 3-tick gap = Add.254), DIRECTLY FALSIFYING strict periodic-3tick prediction at single-tick anchor.

**Updated all-silent regime posteriors (synth #534 anchor)**:
- H_all-silent-cluster (1-tick consecutive sustains): 0.20 → **0.55** (+0.35 under direct cluster confirmation at single-tick anchor with prior P ≈ 0.20 under cluster baseline).
- H_periodic-3tick: 0.40 → **0.20** (−0.20 under direct falsification — Add.252 NOT at 3-tick gap from Add.251).
- H_independent-all-silent: 0.55 → 0.20 (−0.35 — independence model predicts P ≈ 0.024² × small chance for any ordered pair, falsified at cluster signature).
- H_alt: 0.05 → 0.05 (unchanged).

**Cluster vs periodic discriminator chain forward**:
- If Add.253 = all-silent (P ≈ 0.55 under cluster): cluster extends to TRIPLET tier; cluster posterior → 0.75 strong-tier; periodic falsified to 0.10.
- If Add.253 = at-least-1-active: cluster terminates at 2-tick; cluster posterior → 0.40 substantial; periodic neither confirmed nor falsified at single-tick.
- If Add.253 = all-silent AND Add.254 = at-least-1-active: cluster confined to 2-tick instances at 3-tick gap (Add.248-251 + Add.252-?); could re-anchor periodic-3tick with cluster-pair sub-mode.
- If Add.253 = all-silent AND Add.254 = all-silent: cluster QUADRUPLET — cluster posterior → 0.90 decisive-tier; periodic falsified to 0.05.

**Cumulative single-cumulative BF(H_cluster : H_periodic) at synth #534 anchor**: ×4.5 substantial-tier-favoring-cluster (combined ×7.5 vs independence baseline gives joint cumulative BF ≈ ×33.8 strong-tier-favoring cluster vs joint-alt).

## Zero-sustain sub-mode promoted to substantial-tier via Markov-cycle falsification redistribution

Synth #532 instantiated low-zero Markov sub-cycle candidate at single-cumulative BF ×7.1 substantial-favoring-cycle. Add.252 = zero (NOT low) directly falsifies the cycle's discriminator prediction P(Add.252 = low) ≈ 0.55. Posterior mass redistribution at single-tick anchor:

- H_low-zero-cycle: 0.45 → **0.18** (−0.27 under direct discriminator falsification).
- H_uniform-Markov: 0.30 → 0.32 (+0.02).
- H_class-property-attractor: 0.20 → **0.27** (+0.07 — strengthened by zero-triplet within-class invariance, synth #533 anchor).
- H_zero-sustain-sub-mode (NEW from Add.251 candidate, formalised at synth #534): 0.05 → **0.20** (+0.15 — promoted from candidate to substantial-tier under zero-triplet observation).
- H_alt: 0.00 → 0.03 (small reallocation).

**Zero-sustain sub-mode formal hypothesis**: under H_zero-sustain, conditional probability P(zero | zero) is elevated to ~0.50-0.65 (vs uniform ~0.25), and zero-class N→N runs follow geometric distribution with mean 2.5-3.5 instances. Observed run length so far at synth #534 anchor: zero-class run = 2 (Add.251 + Add.252 consecutive). Predicted run-length tail: P(run ≥ 3) ≈ 0.50 under zero-sustain.

**Discriminator chain forward**:
- Add.253 = zero: zero-sustain run extends to 3-instance; sub-mode posterior → 0.40 substantial-strong; cycle posterior → 0.10 marginal.
- Add.253 = low: low-zero cycle re-instantiates at 1-tick discriminator gap; cycle posterior → 0.30 substantial; zero-sustain posterior → 0.10 marginal.
- Add.253 = mid: mid-class second instance; class-property hypothesis strengthens via mid-class invariance test (would test ×3.2 against Add.249 ×3.2).
- Add.253 = high: high-class second instance (Add.246 was first); high-class invariance test.

**Joint zero-sustain + cluster hypothesis**: zero-sustain and all-silent-cluster are **structurally coupled** — zero-class observation requires all-silent (no merges in any carrier). Joint event P(zero ∩ all-silent) at single tick ≈ 1.0 (deterministic coupling at zero-class definition). Therefore zero-sustain run extension implies all-silent cluster extension — they cannot decouple at observable level. Posterior coupling at synth #534: H_zero-sustain implies H_all-silent-cluster, but reverse implication holds only at probability ~0.65 (all-silent does NOT necessarily imply zero merges if non-watched-carrier merges occur — but watched-carrier silence implies zero watched-carrier merges, i.e., zero-class).

**Resolved coupling**: at watched-carrier level, zero-class ⟺ all-silent (perfect 1-1 mapping). Zero-sustain run extension and all-silent cluster extension are the SAME observable event at watched-carrier level.

## Cross-correlation between verdict-axis and merge-rate-axis

Cross-correlation analysis between verdict-axis (review verdict-mix from prior reviews drip) and merge-rate-axis (zero-class triplet observation):

**Recent reviews drip-271 verdict-mix (per dispatcher history Add.250 anchor)**: 0-as-is / 7-after-nits / 1-RC / 0-ND across 8 fresh PRs. Verdict-mix entropy ≈ 0.81 bits (low entropy under after-nits dominance).

**Merge-rate-axis at zero-class triplet**: 0 in-window merges across 0 unique repos × 3-tick depth.

**Cross-correlation hypothesis**: under H_verdict-merge-decoupled (verdict-axis at after-nits dominance is independent of merge-rate-axis at zero-class triplet), joint observation P ≈ 0.50 × 0.30 ≈ 0.15. Under H_verdict-merge-coupled (after-nits dominance correlates with reduced active carrier rotation, leading to lower merge-rate at watched carriers), joint P ≈ 0.45.

**Single-cumulative BF(H_coupled : H_decoupled) at synth #534 anchor**: ×3.0 substantial-tier — but with very limited observation depth (single drip + single triplet anchor), this BF should be regarded as preliminary. Cross-axis verdict-merge coupling requires multi-drip / multi-triplet observation to discriminate at decisive tier.

**Predicted cross-axis discriminator at next reviews drip + next merge-rate observation**:
- If next drip verdict-mix at after-nits dominance + next tick continues zero-class: coupling posterior elevates to substantial-strong.
- If next drip verdict-mix shifts to as-is/RC mix + next tick recovers to mid/high class: coupling posterior elevates similarly (negative-correlation also confirms coupling at opposite-direction).
- If verdict-mix and merge-rate-axis observations vary independently: decoupling posterior strengthens.

## Updated tempered posteriors and projections

- **Anchor-merge persistence sub-mode (synth #534 first-anchor)**: H_persistent-anchor 0.55 / H_independent-anchor 0.30 / H_alt 0.15 (substantial-tier under cumulative BF ×12.1).
- **All-silent cluster vs periodic discrimination (synth #534 promotion)**: H_cluster 0.55 / H_periodic-3tick 0.20 / H_independent 0.20 / H_alt 0.05 (cluster ELEVATED to majority-plurality at single-tick anchor under direct periodic falsification).
- **Zero-sustain sub-mode (synth #534 promotion)**: H_zero-sustain 0.20 / H_low-zero-cycle 0.18 / H_class-property 0.27 / H_uniform-Markov 0.32 / H_alt 0.03 (sub-mode promoted from candidate-tier to substantial-tier under zero-triplet observation; class-property attractor at modal under combined zero-triplet + low-doublet within-class invariance).
- **Verdict-merge cross-axis coupling (synth #534 first-anchor)**: H_coupled 0.40 / H_decoupled 0.45 / H_alt 0.15 (preliminary marginal-tier — discrimination requires multi-drip + multi-triplet observation).

## Predictions deferred to ADD-253 / synth #535

- **Anchor-merge persistence at 4-tick depth**: P(litellm #27039 sustains as anchor at Add.253) ≈ 0.42 (conditional on zero-class sustain at P ≈ 0.50). Cross-axis discriminator at single-tick anchor.
- **All-silent cluster triplet vs cluster-pair termination**: P(Add.253 all-silent) ≈ 0.55 under cluster hypothesis dominance; cluster triplet would elevate cluster posterior to 0.75 strong-tier.
- **Zero-sustain run extension to 3-instance**: P(Add.253 = zero) ≈ 0.50 under zero-sustain sub-mode; would advance zero-class within-class invariance to FOUR-instance tier (synth #533 forward-link).
- **Class-property attractor at modal posterior**: continued zero-class observation strengthens H_class-property to 0.40+ majority-plurality at synth #535 if sustained.
- **Verdict-merge cross-axis discriminator**: requires next reviews drip + next merge-rate observation; preliminary cumulative BF ≈ ×3.0 awaits multi-instance discrimination.
- **Joint cluster + persistent-anchor + zero-sustain triple-confirmation**: P(all three sustain at Add.253) ≈ 0.42 conditional on zero-class continuation. Triple-confirmation would instantiate **structurally-coupled triple-sub-mode** at synth #535 first-anchor with joint posterior elevation to 0.65+ strong-tier.
- **Cross-repo trend extension**: under sustained zero-class regime + persistent-anchor + cluster-extension, the cross-repo trend at watched-carrier level CONSOLIDATES at "all-silent low-frequency state" with structural depth at three coupled sub-modes (cluster + anchor-persistence + zero-sustain). This represents a candidate W17-late-window terminal regime distinct from prior visible-window mid-frequency mixed-class regime.
