# modelcontextprotocol/servers — 2026-03-07

> Reference MCP server implementations; canonical examples for the protocol.

**Window:** 2026-03-06T23:59:59.000Z → 2026-03-07T23:59:59.000Z  
**Default branch:** `main`  
**Source:** [github.com/modelcontextprotocol/servers](https://github.com/modelcontextprotocol/servers)

## Daily summary

> _LLM-generated. May contain errors — click through before acting._

The day's activity in `modelcontextprotocol/servers` was light but cleanup-focused, with three bug-fix PRs merged by `olaservo` and no new issues, releases, or open PRs recorded in the window.

The most consequential merge is **#3297** (`6d977dd`), which fixes `memory` server behavior so that `openNodes` and `searchNodes` actually return the relations connected to the requested nodes. Anyone building knowledge-graph workflows on top of the memory server should pull this — prior responses were effectively returning incomplete subgraphs, which can silently degrade downstream reasoning. This is the change most worth reading in detail today.

Also notable is **#3293** (`a97aba1`), updating the `fetch` server to httpx 0.28+'s new proxy parameter API. This is a compatibility fix rather than a feature change, but it unblocks installs against current httpx versions and should resolve runtime/TypeError reports for users on recent dependency pins. Worth a quick skim if you maintain a fork or vendored copy of `fetch`.

Rounding things out, **#3252** (`ad3e660`) lands in the `everything` reference server, allowing re-registration of session resources. The previous behavior rejected or duplicated re-registrations within a session, which made the example server an unreliable basis for testing dynamic-resource clients. The fix is small but improves fidelity of the canonical example, so client authors using `everything` as a conformance target will want to be aware.

No protocol-level or API-surface changes shipped today; all three merges are scoped bug fixes within individual reference servers (`memory`, `fetch`, `everything`). There are no new community issues to triage and no release cut, suggesting maintainers are in a steady-state cleanup mode rather than preparing a versioned drop. If you only have time for one diff, read **#3297** — it changes returned data shape in a way consumers may have been working around. **#3293** is the next priority for operators hitting httpx upgrade friction.

## Releases

_None in window._


## Merged PRs

_None in window._


## Open PRs (new or updated)

_None in window._


## Notable Issues

_None in window._


## Commits on `main`

- [`a97aba1`](https://github.com/modelcontextprotocol/servers/commit/a97aba19eb218bedd37ae19c27893ee6659f1555) Merge pull request #3293 from thecaptain789/fix-httpx-proxy-arg — _@olaservo_
- [`ad3e660`](https://github.com/modelcontextprotocol/servers/commit/ad3e660cc13b5276639e98303286bf06687331b0) Merge pull request #3252 from dotsetgreg/fix/session-resource-reregistration — _@olaservo_
- [`6d977dd`](https://github.com/modelcontextprotocol/servers/commit/6d977ddc62da8aa851cdbbc56e9198ca985a5f45) Merge pull request #3297 from DukeDeSouth/fix/memory-open-nodes-relations — _@olaservo_


---
_Generated by [oss-digest](https://github.com/Bojun-Vvibe/oss-digest) · v0.2 (LLM summary + deterministic detail)._
