# cline/cline — 2026-03-14

> VS Code-native autonomous coding agent; another adjacent ecosystem signal.

**Window:** 2026-03-13T23:59:59.000Z → 2026-03-14T23:59:59.000Z  
**Default branch:** `main`  
**Source:** [github.com/cline/cline](https://github.com/cline/cline)

## Daily summary

> _LLM-generated. May contain errors — click through before acting._

A quiet day in cline/cline: no releases, no commits to main, no new issues, and the four PRs touched in the window were all **closed without merge**. There is nothing here that ships to users, but the closures are worth a quick note for anyone tracking dependency hygiene or checkpoint reliability.

The most consequential closure is the Aikido autofix PR #9785, which proposed upgrading `simple-git`, `hono`, `@aws-sdk/xml-builder` and seven other deps to resolve **20 CVEs, including 3 critical** (RCE in git command execution, authz bypass in static file serving, XSS via XML entity shadowing). It was closed unmerged after sitting open ~2 days — presumably superseded by another upgrade path, but worth confirming the critical CVEs are addressed elsewhere before assuming the repo is clean. The smaller sibling PR #9713 (3 CVEs in `preact`, `dompurify`, `mermaid`, `lodash-es`) was closed at the same time, suggesting a coordinated cleanup of stale Aikido bots rather than acceptance of the fixes.

On the engineering side, #9807 ("consolidate parallel tool-calling and vertex snapshot output") from arafatkatze was opened and closed the same day without merging — a bundled fix for parallel tool-calling behavior plus a Vertex Gemini 3 snapshot newline update. Short-lived, so likely refactored into a different PR; worth watching for a follow-up if you depend on parallel tool calls or Vertex.

Finally, #9317 from echoVic — a month-old fix for the checkpoint system corrupting the workspace's root `.git` directory (issue #9286) — was also closed unmerged. The bug description is substantive: `renameNestedGitRepos(false)` was incorrectly renaming the root-level `.git_disabled` back during restore because the `ignore: [".git"]` glob doesn't match `.git_disabled`. If you've seen checkpoint-related git corruption reports, the underlying issue #9286 may still be live.

Net: no merges, but #9785 (critical CVEs) and #9317 (checkpoint corruption) are the threads to pull on to confirm whether the underlying problems are resolved elsewhere.

## Releases

_None in window._


## Merged PRs

_None in window._


## Open PRs (new or updated)

_None in window._


## Closed PRs (not merged)

- [#9785](https://github.com/cline/cline/pull/9785) **[Aikido] Fix 20 security issues in simple-git, hono, @aws-sdk/xml-builder and 7 more** — _by @aikido-autofix[bot], closed 2026-03-14T23:35:03Z_
- [#9713](https://github.com/cline/cline/pull/9713) **[Aikido] Fix 3 security issues in preact, dompurify, mermaid and 2 more** — _by @aikido-autofix[bot], closed 2026-03-14T23:34:57Z_
- [#9807](https://github.com/cline/cline/pull/9807) **fix: consolidate parallel tool-calling and vertex snapshot output** — _by @arafatkatze, closed 2026-03-14T20:31:38Z_
- [#9317](https://github.com/cline/cline/pull/9317) **fix: prevent checkpoint system from corrupting root .git directory (#9286)** — _by @echoVic, closed 2026-03-14T15:24:40Z_


## Notable Issues

_None in window._


## Commits on `main`

_None in window._


---
_Generated by [oss-digest](https://github.com/Bojun-Vvibe/oss-digest) · v0.2 (LLM summary + deterministic detail)._
