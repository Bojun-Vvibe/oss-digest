# ADDENDUM-139 — 2026-04-29 (capture 2026-04-29T02:25:14Z)

**Capture window:** 02:03:45Z → 02:25:14Z UTC. Window width: **21m29s** (Add.138 was 41m45s — **0.51× contraction**, sharp halving and the **shortest window-width since the Add.135-136 medium-class doublet was broken**; the short-class regime that held at lag-2 across Add.137-138 not only persists but contracts further to **ultra-short class** at lag-3, breaking from the ~40m plateau).
**Predecessor:** ADDENDUM-138 (closed 02:03:45Z). Carry-pred resolutions this tick: codex same-author-recurrence regime (synth #307) (tick 1→**REFUTED**: codex emits **0 in-window merges** — celia-oai self-doublet does NOT chain at lag-1, canvrno-oai does NOT chain again, the entire Add.138 same-author-recurrence regime collapses into **codex full silence at lag-1**; codex backbone-pair role broken for first time in the Add.136-138 3-tick run); litellm vendor-suffix monoculture (synth #308) (tick 1→**REFUTED-BY-SILENCE**: litellm emits **0 merges** — yuneng-berri does NOT chain at lag-1 to make n=4, the n=3-consecutive-tick monoculture **bounded** at exactly the run length where synth #308 codified it; litellm enters first dormancy tick since Add.135); qwen-code drop-out streak (tick 1→**BROKEN**: qwen-code emits **1 merge** — wenshao #3687 `1b9e8ec4` 02:10:42Z `feat(core): wire background shells into the task_stop tool` — wenshao is the **synth #145 anchor author** (qwen-code background-shell-pool series, originally #3637/#3642), the **n=3 drop-out streak terminates at exactly the prior W17 record without extending**; wenshao breaks silence as a **same-author anchor-series continuation** semantically chained to the original #3642 `managed background shell pool with /bashes command` — `wire background shells into the task_stop tool` is a **direct downstream feature on the same background-shell subsystem** at multi-day lag); gemini-cli singleton-then-redormant (tick 1→**SUSTAINED**: 0 merges, depth post-#26150 now ~1h12m+, the singleton-then-redormant cadence sub-mode persists at lag-2); opencode silence (tick 1→**SUSTAINED**: 0 merges, depth post-Hona #24864 now **2h47m+**, n=3 drop-out streak — opencode joins qwen-code's prior n=3 record); goose silence (tick 1→**SUSTAINED**: 0 merges, depth post-morgmart #8881 now **4h51m+**, **n=6 drop-out streak — extends W17 record by another tick**).

## Headline events — **codex+litellm backbone collapses to dual-silence at lag-1; qwen-code lone-merger via wenshao synth-#145-anchor reactivation; 5-of-6 repos dormant — broadest-silence tick of the Add.119-139 band**

Cross-repo merge count this window: **1 in-window merge** over 02:03:45Z → 02:25:14Z. Per-minute merge rate **0.0466** (Add.138 was 0.0958 — **0.49× contraction**, halving for the second consecutive tick). Trajectory Add.119–Add.139 = 0.000 → 0.107 → 0.182 → 0.279 → 0.120 → 0.020 → 0.051 → 0.133 → 0.110 → 0.0865 → 0.1222 → 0.0828 → 0.1487 → 0.0958 → **0.0466**. Per-repo: codex 0, litellm 0, gemini-cli 0, opencode 0, goose 0, qwen-code 1. Active-repo-count **1** (Add.138 was 2 — **further contraction to 1**, the **lowest active-repo-count of the entire Add.119-139 band**, undercutting the prior Add.124 trough). The active repo is **qwen-code, not the codex+litellm backbone** — the active-repo identity flips for the first time in the Add.136-139 4-tick span.

1. **wenshao #3687 `1b9e8ec4` 02:10:42Z `feat(core): wire background shells into the task_stop tool`** — qwen-code's sole in-window merge, and the sole cross-repo merge. Subsystem: core / background-shells / task_stop tool. Author class: wenshao is **deep-W17-recurring anchor of synth #145** (qwen-code #3637 reasoning-content + #3642 `managed background shell pool with /bashes command`). Title-class: `feat(core):` Conventional Commits with scope. **Semantic anchor-chain at multi-day lag**: #3687 `wire background shells into the task_stop tool` is a **direct downstream feature on the same background-shell subsystem** that wenshao introduced at #3642 — the `/bashes` command and the `task_stop` tool both operate on the same background-shell-pool runtime object. This is a **same-author same-subsystem extension at multi-day lag** (synth #145 was anchored mid-W17; current is end-W17), distinct from same-tick or lag-1 same-author patterns — a **deep-lag intra-author subsystem-extension** sub-mode.

2. **codex full silence at lag-1 — synth #307 same-author-recurrence regime collapses.** Add.138 had codex emit 3 merges in same-author-recurrence regime (canvrno-oai chain, celia-oai self-doublet). Add.139 codex emits **0**. Neither celia-oai nor canvrno-oai chains forward; the same-author-recurrence regime that synth #307 framed as the structural inverse of synth #306's broadening-pool **fails to extend at the first lag-1 test**. codex now exhibits its **first full-silence tick since Add.131** (n=8-tick activity streak broken). Synth #307's "contraction-via-recurrence" framing was a **single-tick regime, not a sustained one** — the regime above the regime is **codex cadence is itself decaying toward silence** across Add.137 (0.1487/min) → Add.138 (0.0958/min, 75% codex-concentrated) → Add.139 (0/min for codex).

3. **litellm full silence at lag-1 — synth #308 vendor-suffix monoculture bounded at n=3.** Add.138 had yuneng-berri #26731 extending the -berri monoculture to n=3 consecutive ticks. Add.139 litellm emits **0**. yuneng-berri does NOT chain to n=4 same-author-back-to-back, and no other -berri author appears. The n=3-consecutive-tick vendor-suffix monoculture that synth #308 codified is **empirically bounded at exactly n=3** — same-shape termination as synth #306's n=4 broadening-pool bound, suggesting **W17 author-class-monoculture regimes terminate at small finite n** (n=3 for litellm vendor-suffix, n=4 for codex broadening-pool) rather than running indefinitely.

4. **codex+litellm backbone-pair collapses to dual-silence — first such event in Add.136-139 4-tick run.** Add.136-138 had codex+litellm as the only stable concurrent-active backbone (codex always non-zero, litellm always non-zero, even when other repos varied). Add.139 has **both at zero simultaneously** — the backbone-pair stability hypothesis (implicit across Add.136-138) is falsified. The repo that fills the activity vacuum is **qwen-code, which had been silent for n=3 ticks** — a **role-rotation** where the previously-silent repo becomes the lone active and the previously-backbone pair both go silent.

5. **qwen-code n=3 drop-out streak terminates at exactly the prior W17 record — no extension.** Add.122-124 was the prior qwen-code n=3 record; Add.136-138 was qwen-code dormancy at n=3. Add.139 breaks silence at exactly the same length as the prior record, not extending it. The **W17 qwen-code drop-out ceiling holds at n=3** for now (vs goose's n=6-and-still-extending). The break is **not a generic re-activation** but a **synth #145 anchor-author re-emergence** — qwen-code's silence-break is **author-class-narrow** (single author, the deep-W17 anchor) rather than fresh-author or pool-broadening.

6. **opencode drop-out streak EXTENDS to n=3 ticks — joins qwen-code's prior W17 record.** No opencode merges — depth post-Hona #24864 now **2h47m+**. opencode now matches the qwen-code prior W17 record at n=3 drop-out streak. The Add.136 Hona n=2 same-author back-to-back never extended, and the post-Hona silence is now structurally identical to the qwen-code mid-W17 silence pattern.

7. **goose drop-out streak EXTENDS to n=6 ticks (W17 record extends again).** No goose merges — depth post-morgmart #8881 now **4h51m+** at capture (Add.138 4h29m+ → Add.139 4h51m+). The W17 record set last tick at n=5 now stands at **n=6**. goose silence is now **2× the qwen-code/opencode prior-record ceiling**, structurally distinct as a deeper dormancy class.

8. **Gemini-cli singleton-then-redormant cadence sub-mode persists at lag-2.** No gemini-cli merges — depth post-#26150 now ~1h12m+. The Add.135-and-Add.137 n=2 instances of singleton-then-redormant now have a **2-tick post-singleton dormancy span** for the Add.137 instance, matching the Add.135-136 instance's 1-tick post-singleton dormancy at lag-1 and now exceeding it at lag-2.

9. **Broadest-silence tick of the entire Add.119-139 band.** Active-repo-count = 1 is the **trough of the band** (prior trough was Add.124 at active-count 1 with a different active repo). Cross-repo merge-count = 1 is also a **band trough**. The active-repo-identity is qwen-code (not codex/litellm), and the lone merger is a **deep-anchor-author silence-break**, not a fresh debut or backbone-continuation. This tick has the **most concentrated activity (1 merge, 1 author, 1 repo)** of any tick in the band.

## Per-repo silence/activity tally (this window)

| Repo | In-window merges | Dormancy duration | State |
|---|---|---|---|
| qwen-code | 1 (wenshao #3687) | — | n=3 drop-out streak terminates at prior W17 record; synth #145 anchor-author silence-break with same-subsystem multi-day-lag extension |
| codex | 0 | ~25m+ | First full-silence tick since Add.131; synth #307 same-author-recurrence regime collapses at lag-1 |
| litellm | 0 | ~25m+ | First full-silence tick since Add.135; synth #308 vendor-suffix monoculture bounded at n=3 |
| gemini-cli | 0 | ~1h12m+ | Singleton-then-redormant cadence sub-mode persists at lag-2 |
| opencode | 0 | 2h47m+ | **n=3 drop-out streak — matches prior W17 qwen-code record** |
| goose | 0 | 4h51m+ | **n=6 drop-out streak — W17 record extends again** |

## Window shape

Window width contracts to 21m29s (0.51× Add.138, ultra-short-class break from the ~40m plateau). Active-repo-count contracts to 1 — the **band trough**. The codex+litellm backbone-pair stability hypothesis (implicit across Add.136-138) collapses with both at zero concurrently for the first time. Synth #307's same-author-recurrence regime fails to extend at lag-1 (codex full silence). Synth #308's vendor-suffix monoculture is bounded at n=3 (litellm full silence) — paralleling synth #306's n=4 broadening-pool bound, suggesting **W17 author-class-monoculture regimes terminate at small finite n**. The lone merger is **wenshao #3687** in qwen-code, a **synth #145 anchor-author silence-break with a multi-day-lag same-subsystem extension** (`task_stop` wires into the `/bashes` background-shell-pool that wenshao introduced at #3642). qwen-code's n=3 drop-out streak terminates at exactly the prior W17 record without extension. opencode joins the n=3 record class. goose extends its drop-out record to n=6 (now 2× the n=3 ceiling). The active-repo identity flips from codex/litellm to qwen-code — a **role-rotation** where backbone goes silent and previously-silent repo becomes lone active.
