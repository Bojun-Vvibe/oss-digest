# modelcontextprotocol/servers — 2026-03-28

> Reference MCP server implementations; canonical examples for the protocol.

**Window:** 2026-03-27T23:59:59.000Z → 2026-03-28T23:59:59.000Z  
**Default branch:** `main`  
**Source:** [github.com/modelcontextprotocol/servers](https://github.com/modelcontextprotocol/servers)

## Daily summary

> _LLM-generated. May contain errors — click through before acting._

A single drive-by PR (#3737) opened and immediately closed (unmerged) within about 40 seconds is the only activity in the 24-hour window for `modelcontextprotocol/servers` — no merges, no releases, no new issues, no commits to `main`.

## What happened

The lone signal is **PR [#3737](https://github.com/modelcontextprotocol/servers/pull/3737)** by `32134178csa`, "fix(filesystem): improve edit_file error with nearest-match diagnostics." The author proposed enriching the failure message produced by the filesystem server's `edit_file` tool when neither exact nor whitespace-flexible matching succeeds. Today, that error simply echoes the supplied `oldText` with no context, which makes it hard for agents (and humans) to recover. The PR's stated improvements were to include the file path in the message, point to the closest near-match location with 1-indexed line numbers, report a match percentage (lines matching after trimming), and presumably show a line-by-line diff snippet (the description is truncated mid-bullet).

The PR was closed less than a minute after opening, with no merge and no review activity captured in the window. The JSON does not indicate whether the author closed it themselves (likely, given the timing — perhaps to rework, resubmit against a different base, or because a duplicate exists) or whether a maintainer rejected it. There is no linked issue and no labels were applied.

## Why it might still matter

Even though nothing landed, the underlying ergonomics problem is real and recurring: agent loops frequently get stuck retrying `edit_file` against subtly mismatched `oldText` (trailing whitespace, CRLF, indentation drift), and better diagnostics would meaningfully reduce wasted tool calls. If you maintain a downstream fork of the filesystem server or are tracking agent reliability issues, it's worth watching for a re-opened version of this change or filing an issue to capture the idea before it's lost.

## What to read

- Only [#3737](https://github.com/modelcontextprotocol/servers/pull/3737), and only briefly — mainly to see whether the author follows up. Otherwise, this is a quiet day in the repo and can be safely skipped.

## Releases

_None in window._


## Merged PRs

_None in window._


## Open PRs (new or updated)

_None in window._


## Closed PRs (not merged)

- [#3737](https://github.com/modelcontextprotocol/servers/pull/3737) **fix(filesystem): improve edit_file error with nearest-match diagnostics** — _by @32134178csa, closed 2026-03-28T04:47:58Z_


## Notable Issues

_None in window._


## Commits on `main`

_None in window._


---
_Generated by [oss-digest](https://github.com/Bojun-Vvibe/oss-digest) · v0.2 (LLM summary + deterministic detail)._
